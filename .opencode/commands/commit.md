---
description: 'Erstellt einen Git-Commit nach den Projektkonventionen mit Safety-Check und selektivem Staging'
---

Führe den OpenCode-Workflow `git-commit` aus.

ANWEISUNGEN:

1. LADE `{project-root}/.agents/skills/git-commit/SKILL.md`
2. LESE den vollständigen Inhalt
3. BEFOLGE den Workflow exakt
4. NUTZE vor dem Schreiben der Commit-Message zusätzlich `{project-root}/.agents/skills/_shared/commit-conventions.md`
5. STAGE nur explizit relevante Dateien, niemals pauschal alles

SKILL-DATEI: `{project-root}/.agents/skills/git-commit/SKILL.md`
