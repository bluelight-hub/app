# Agent Customization
# Customize any section below - all are optional

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

# Add custom critical actions (appended after standard config loading)
critical_actions:
  - "use sub agents"
  - "Load and strictly follow {project-root}/docs/architecture-principles.md as the authoritative source for all architecture decisions"

# Add persistent memories for the agent
memories:
  - "The project has architecture principles documented in {project-root}/docs/architecture-principles.md — this is the authoritative source of truth for all architectural decisions, patterns, and constraints. Always load this document first when making or reviewing architecture choices."

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - trigger: my-workflow
#     workflow: "{project-root}/custom/my.yaml"
#     description: My custom workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: my-prompt
#   content: |
#     Prompt instructions here
