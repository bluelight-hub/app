# Deferred Work

Offene, bewusst aufgeschobene Punkte aus Code-Reviews und Retros. Jeder Eintrag gehört zu einem bestimmten Review/Retro — vor dem Abarbeiten zurück zur Quelle springen und Kontext mitnehmen.

## Deferred from: code review of story-3.5 (2026-04-24)

- **handleQuittieren mid-flight close → onQuittieren feuert für gecancelte Einheiten** [`PsaProfilDetailDrawer.tsx:1062–1080`] — Single-Einheit-MVP-Pfad in Story 3.5 trifft den Bug nicht; Story-3.6+/Multi-Einheit-Erweiterung muss `cancelled`-Flag oder AbortController einführen.
- **`useAckPsaQuittung` doppelte Hook-Instanziierung** [`PsaProfilEmpfangBanner.tsx:1387` + `PsaProfilDetailDrawer.tsx:1047`] — Banner und Drawer instanziieren beide den Mutation-Hook. TanStack-Query-Cache wird via `queryClient` geteilt; akzeptables Pattern. Prüfen, ob Optimistic-Dismiss zwischen den beiden Instanzen identisch verläuft (kein Doppel-Optimistic).
- **`state.checked` behält stale Composite-Keys nach `aktiveProfile`-Reduktion** [`use-equipment-checklist-state.ts:493–544`] — Wenn `aktiveProfile` mid-flight schrumpft, bleiben Item-Keys verwaister Profile in der Map. Kein realer Pfad in Story 3.5 (aktiveProfile pro Banner-Tap stabil); Multi-Stunden-Sessions mit häufigem Profil-Toggle könnten unbounded wachsen.
- **Future Risk: Doppelte Checkbox-IDs bei Item-Überschneidung mehrerer Profile** [`EquipmentChecklist.tsx:792–807`] — Heute keine Item-ID-Überlappung (BE-Sync-Test garantiert eindeutige IDs). Bei zukünftiger Überlappung würden zwei Checkboxen denselben HTML-`id` tragen → A11y-Violation. De-dup-Render-Pfad im Organism vorsehen.
- **Touch-Target auf bare `<input type="checkbox">` 20×20 px (Label-wrapped 44+)** [`EquipmentChecklist.tsx:800–808`] — Label macht den Klick-Bereich groß genug, aber iOS-Safari-Edge-Cases (Tap auf nackte Checkbox vs Label) sollten in Cross-Story-A11y-Audit (Epic 7) verifiziert werden.
- **`bulkEinheitIds` recomputed jedes Render** [`PsaProfilePage.tsx:2115–2126`] — Pre-existing aus Story 3.2; Spread auf `selection.selectedEinheitIds` legt Array bei jedem Render neu an. `useMemo` wäre die saubere Variante.
- **Fade-Timer `triggerFade` extends old fade auf Re-Trigger** [`PsaProfilePage.tsx:2073–2095`] — Pre-existing aus Story 3.4. Zweiter `triggerFade`-Call innerhalb der Fade-Periode verlängert die ganze Set-Fade um die volle Dauer (statt pro-ID-Timer). Visuell akzeptabel, aber Verhalten ist nicht getestet.
- **`formatTime`/`formatBekanntgabeZeit` lokale Zeitzone** [`PsaProfilEmpfangBanner.tsx:1623–1633`, `PsaProfilePage.tsx:2316–2326`] — Pre-existing, Cross-Story. Verteiltes Lagezentrum sieht je nach Client-TZ unterschiedliche Zeitanzeigen. Konsistente Lösung sollte mit Cross-Story-Hardening / Epic 7-Polish kommen.
- **`aggregateItems` dreifach dupliziert** (`buildAggregierteCheckliste` BE + `aggregateChecklisten` Hook + `aggregateItems` Organism) — Strukturgleiche De-Dup-Funktionen. Refactor-Kandidat für Phase 2 / FR16, wenn Mapping ohnehin auf API-Hook umgestellt wird.
- **JSDoc-Inkonsistenz `pristine` vs. „1..n-1" Beschreibung** [`use-equipment-checklist-state.ts:480–492`] — Dokumentation des Status-Reducer-Verhaltens unscharf bei „0 Items aggregiert" (auch `pristine`). Kosmetisch.
- **`<article aria-pressed>` in `PsaProfilePage`** [`PsaProfilePage.tsx:2392`] — Pre-existing, nicht Story-3.5. `aria-pressed` ist auf Button-Rolle definiert; auf `<article>` non-standard. Cross-Story-A11y-Audit.
- **Drawer mounted unconditionally → Hooks laufen mit Empty-PropagationGroupId** [`PsaProfilDetailDrawer.tsx:1044–1056`] — Wird durch Reset-Effekt aus Patch P2 (`useEffect(() => dispatch({ type: 'reset' }), [propagationGroupId])`) entschärft. Kein zusätzlicher Patch nötig sobald P2 angewendet ist.

## Deferred from: code review of story-3.4 (2026-04-28)

- **Re-Ack von anderem User wird stumm akzeptiert** [`packages/backend/src/application/eigenschutz/commands/ack-psa-quittung/ack-psa-quittung.handler.ts:387`] — Bei `created: false` wird der `caller`-User verworfen, kein Audit-Log-Eintrag bei Mismatch zur ursprünglichen `quittiertVonUserId`. Kein Spec-Bruch, aber Audit-Trail-Lücke. Story 3.6 (Lücken-Meldung) oder Story 3.11 (Telemetrie) sollten klären.
- **`AckPsaQuittungResult.alreadyAcknowledged`-Flag wird ignoriert** [`packages/backend/src/modules/eigenschutz/controllers/psa-profil.controller.ts:3049`] — Controller mappt sowohl `created` als auch `alreadyAcknowledged` auf 204. Entweder als `X-Idempotent-Replay`-Header für Client-Telemetrie nutzen oder Feld entfernen — leakende Abstraktion.
- **`occurredAt`-Sortierung via `localeCompare`** [`list-offene-psa-bekanntgaben.handler.ts:1034`] — Funktioniert für UTC-Zulu-Suffixes, theoretisches Risiko bei Offset-Strings (`+02:00`). System produziert aktuell nur Zulu — kein aktiver Bug.
- **`fetchAffectedBulk` Per-Einheit-Fehler-Toleranz unvollständig** [`psa-profil.controller.ts:3171-3183`] — Wirf der QueryBus statt Result.fail, kippt der ganze Bulk-Response. Betrifft Story-3.1/3.2-Bulk-Pfad, nicht Story-3.4-Quittung.
- **Tx-Mock ohne `outboxEvent`-Delegate produziert stille NotFound** [`ack-psa-quittung.handler.ts:427-453`] — Defensive: bei fehlendem Delegate sollte Invariant-Failure statt false. Test-Mock-Hardening.
- **`resolveSeit` akzeptiert Future-ISO ohne Hinweis** [`list-offene-psa-bekanntgaben.handler.ts:1038-1046`] — Caller mit zukünftigem `seit` bekommt leere Liste; explizites `ValidationFailed:seitInFuture` wäre sauberer.
- **N+1 bei sequenziellem `findByEinsatzAndGroup`** [`list-offene-psa-bekanntgaben.handler.ts:993-1031`] — Pro offene Gruppe ein DB-Roundtrip. Bei ≤10 Gruppen typisch unkritisch; Promise.all-Batch oder Single-Query mit groupId IN (...) wäre Optimierung.
- **Orphan-Quittungen werden nicht geloggt** [`list-offene-psa-bekanntgaben.handler.ts:1004-1015`] — Quittung-Row mit `einheitId` außerhalb des erwarteten Empfänger-Sets wird ignoriert ohne Warning. Stale-Daten aus alten Bekanntgaben bleiben unbemerkt.
- **Adapter-Broadcast-Promise nicht awaited** [`packages/backend/src/infrastructure/eigenschutz/event-adapters/psa-quittung-abgegeben.adapter.ts:1862-1890`] — Hängender Broadcast könnte Event-Loop nach Modul-Disposal blockieren. AbortController + Timeout wäre defensiver.
- **`deserializeQuittungAbgegeben` prüft Whitespace-only Strings nicht** [`event-deserializer.ts:2494-2513`] — Korrupte Payloads mit nur Whitespace passieren. `.trim().length > 0`-Check ergänzen.
- **`serializeQuittungAbgegeben` prüft `Date.getTime()` NaN nicht** [`event-serializer.ts:2588-2606`] — `new Date('invalid')` ist `instanceof Date`; Outbox speichert `'Invalid Date'`-ISO. Defensive Validation einbauen.
- **`einsatzId`-Wechsel-Effekt resettet Dedup vor Connect-Cleanup** [`use-eigenschutz-psa-quittung-live.ts:4285-4288`] — Race: alter Socket emittiert Frame nach Reset, Duplikat wird verarbeitet. Reset in Connect-Effekt-Setup verschieben.
- **`MAX_RETRIES`-`error`-Status ohne User-Recovery-Pfad** [`use-eigenschutz-psa-quittung-live.ts:4309-4324`] — Sender sieht stuck `error` ohne manuellen Reconnect-Knopf. Folge-UX-Polish.
- **Initial `connecting`-Status auch wenn `enabled=false` aber `einsatzId` existiert** [`use-eigenschutz-psa-quittung-live.ts:4263-4272`] — Kosmetisch; Status-Anzeige zeigt fälschlich `connecting` bevor Effekt läuft.
- **`useAckPsaQuittung` ohne explizite retry-Policy** [`queries.ts:4111-4119`] — Default-Retry feuert evtl. bei BusinessRule-Verletzung erneut. Bei 4xx kein Retry sinnvoll.
- **`useEigenschutzPsaQuittungen` ohne `refetchOnWindowFocus`-Override** [`queries.ts:4150-4163`] — Bei verpasstem WS-Frame sieht Sender 15s veralteten Counter.
- **`handleAcknowledge` stale-Closure-Risiko auf `ackErrors`/`emittedRef`** [`PsaProfilEmpfangBanner.tsx:5172-5199`] — Funktional korrekt durch `setAckErrors(prev => ...)`; `emittedRef`-Cleanup-Effect kann ack-Group entfernen während Mutation läuft → Re-Emit von `all_banners_delivered` nach Failure-Re-Banner. Subtile Race.
- **`emittedRef`-Cleanup zu eifrig bei Re-Render-Lücken** [`PsaProfilEmpfangBanner.tsx:5139-5146`] — Banner verschwindet kurz und kommt zurück → `all_banners_delivered` doppelt emittiert. Grace-Period oder explizites Dismiss-Tracking.
- **`formatHoursMinutes` nutzt local timezone** [`AcknowledgmentStatusBadge.tsx:4683-4690`] — Stab und Empfänger in unterschiedlichen Timezones sehen unterschiedliche HH:mm. Konsistent mit Story 3.3 (Banner formatTime), eigene Multi-TZ-Story.
- **`deriveAcknowledgmentStatus` liefert 'pending' bei `total=0`** [`AcknowledgmentStatusBadge.tsx:4669-4676`] — Spec AC12 sagt es sollte nie eintreten; defensive Annahme valide. `complete`-Pfad nie für leere Liste; akzeptiert.
- **Popover Focus-Restore bei Esc nicht explizit verifiziert** [`AcknowledgmentStatusBadge.tsx:4750-4801`] — Headless-UI-Default sollte greifen; explizites Test-Assertion fehlt.
- **`formatBekanntgabeZeit` ohne Datums-Anzeige bei >24h** [`PsaProfilePage.tsx:5915-5925`] — Bei 23:59-Bekanntgabe zeigt Liste am nächsten Tag nur HH:mm — verwirrend. MVP-Scope OK.
- **Mutation-Test `silentError`-Cache-Invalidate-Pfad nicht abgedeckt** [`use-ack-psa-quittung.spec.tsx`] — Bei Error darf onSuccess nicht laufen. Test-Schärfung.
- **`@Post quittieren` ohne `@ApiBadRequestResponse`** [`psa-profil.controller.ts:3038-3058`] — OpenAPI-Client kennt 400-Pfad nicht; Frontend muss raten. Decorator-Detail.
- **Aggregation behandelt `aktion=DEAKTIVIERT` semantisch wie `AKTIVIERT`** [`list-offene-psa-bekanntgaben.handler.ts:962-986`] — Reine Deaktivierungs-Bekanntgabe erscheint als „offen", obwohl sie keinen aktiven PSA-Schutz erfordert. Semantische Klärung mit PO/UX (vermutlich gewollt — Empfänger soll bestätigen, dass ihm bewusst ist, was abgenommen wurde).
- **`QuittungAbgegebenEvent.quittiertAm` aus Repo-Eingabe statt DB-Default** [`ack-psa-quittung.handler.ts:399`] — Bei P2002-Race trägt Event evtl. Zeitstempel der Caller-Action statt der ursprünglichen Insert-Time. Konsistenz-Frage.
- **WS-Replay nach Reconnect mit altem `einsatzId`** [`use-eigenschutz-psa-quittung-live.ts:4382-4392`] — Frames aus vorherigem `einsatzId` nach Wechsel können Cache-Invalidate auslösen. Timestamp-Filter sinnvoll.
- **Tx-Rollback-Test prüft kein echtes Rollback** [`ack-psa-quittung.handler.spec.ts:203-211`] — Test asserted nur `result.isFailure` und `outboxRepo.save not called`. Echte Rollback-Verifikation (`$transaction`-Reject + Storage-State) fehlt.
- **AckHandler-Outbox-Lookup ohne `payload.einsatzId`-Filter (Defense-in-Depth)** [`ack-psa-quittung.handler.ts:427-453`] — Theoretisch durch cuid2-Entropie abgedeckt; explizite Filterung wäre defensiver.
- **AC12 axe-Snapshot-Test fehlt** [`AcknowledgmentStatusBadge.spec.tsx`] — Spec AC16 verlangt explizit `axe-Snapshot`. Aktuell nur aria-label/keyboard-Tests.
- **`propagationGroupIdCandidate`-Feld-Name für confirmed groupId** [`telemetry-queue.ts`, Story 3.3-Konvention] — Der „Candidate"-Suffix impliziert non-resolved id; Hook stuffed real groupId. Für Story 3.11 evtl. Schema-Split.
- **Controller-Routen-Reihenfolge nur durch Kommentar gesichert** [`psa-profil.controller.ts:3072,3103`] — `offene-bekanntgaben` muss vor `:propagationGroupId/quittungen` registriert sein. Refactor-Mover könnte still brechen. E2E-Test gegen echten Router würde Reihenfolge locken.

## Deferred from: code review of story-3.3 (2026-04-24)

- **`getBaseUrl()` HTTP-Fallback** [`packages/frontend/src/features/eigenschutz/api/use-eigenschutz-psa-live-banner.ts:161`] — Fällt auf `http://localhost:3091` zurück; CLAUDE.md verlangt HTTPS-only. Pre-existing Pattern aus Story 2.7. Globaler HTTPS-Switch wäre eigene Plattform-Story.
- **`invalidateLists()` doppelt bei Reconnect-Storm** [`packages/frontend/src/features/eigenschutz/api/use-eigenschutz-psa-live-banner.ts:295,312`] — Bei jedem `connect`-Event UND jedem empfangenen Frame wird die Detail-Query invalidiert; in einem Reconnect-Loop erzeugt das mehrere Refetches/Sek. Pattern aus Story 2.7 1:1 übernommen, kein Throttling. Performance-Optimierung außerhalb Story-3.3-Scope.
- **Overflow-Banner-Klick öffnet nur erstes Element** [`packages/frontend/src/features/eigenschutz/ui/organisms/PsaProfilEmpfangBanner.tsx:730`] — Bei N>4 sichtbaren Bannern verlinkt der Sammel-Banner nur `banner[MAX_VISIBLE]`; weitere Overflow-Einträge sind ohne UI-Pfad. Spec markiert die volle Liste explizit für Story 3.4 (Quittungs-Stand-Anzeige) — Stub-Verhalten zulässig.
- **Begründung bei DEAKTIVIERT-Toggles fehlt** [`packages/frontend/src/features/eigenschutz/ui/organisms/PsaProfilEmpfangBanner.tsx:754-764`] — `usePsaProfileByEinheit` liefert nur **aktive** PSA-Profile; für Deaktivierungen findet `profile?.find(...)` nichts und der Banner zeigt nur die Toggle-Summary statt der Backend-Begründung. Q3-Default deckt das mit Fallback. DTO-Erweiterung um `letzteAenderungBegruendung` (oder eigene historische Query) als Folge-Story.
- **`dismiss()` rein lokal — keine Cross-Tab-Sync** [`packages/frontend/src/features/eigenschutz/api/use-eigenschutz-psa-live-banner.ts:210-212`] — Bei Sicherheitsbeauftragter:in mit gleichem Einsatz auf zwei Geräten bleibt der Banner auf dem zweiten Gerät sichtbar. Echte Quittung (Backend-Roundtrip + Cross-Tab-Sync) folgt in Story 3.4.
- **`PSA_PROFIL_META`-Vollständigkeit bei künftigem Profil-Enum** [`packages/frontend/src/features/eigenschutz/ui/organisms/PsaProfilEmpfangBanner.tsx:742`] — Map ist statisch; ein neues Backend-Enum würde aktuell nichts crashen (Zod-Schema schützt), aber zu silent-discarded Frames führen. Frontend-Bump bei Profil-Erweiterung ist sowieso nötig — kein aktiver Bug.
- **LRU-Größe 200 erlaubt theoretisch Re-Eintritt evictierter `eventId`s** [`packages/frontend/src/features/eigenschutz/api/use-eigenschutz-psa-live-banner.ts:97`] — Bei Bursts >200 Events innerhalb derselben Session können Duplikate durchkommen. 200 ist für realistische Einsätze ausreichend; Backfill-Replay (Story 3.11) deckt den Edge-Case ab.
- **`formatTime` ohne Timezone-Hinweis** [`packages/frontend/src/features/eigenschutz/ui/organisms/PsaProfilEmpfangBanner.tsx:791-801`] — `HH:mm:ss` in Client-Zeitzone ohne Suffix; bei Multi-Standort-Einsätzen verwirrend. Nicht MVP-Scope.
- **`transports: ['websocket']` ohne Polling-Fallback** [`packages/frontend/src/features/eigenschutz/api/use-eigenschutz-psa-live-banner.ts:304`] — In WS-blockierten Netzen schlägt die Verbindung dauerhaft fehl. Konsistent mit Story-2.7-Pattern; Polling-Fallback wäre globale Plattform-Entscheidung.

## Deferred from: code review of story-1.1 (2026-04-21)

- **`PrismaService`-Getter-Pattern** — `pushSubscription`-Getter in `packages/backend/src/infrastructure/database/prisma.service.ts:241-244` exponiert den internen Prisma-Client ohne Repository-Wrapper. Das folgt dem existierenden Projekt-Pattern für alle bisherigen Modelle; eine saubere Hexagonal-Kapselung würde alle Getter betreffen und ist eigener Refactoring-Scope.
- **Controller-Pfad vs. `globalPrefix`/`versioning`** — `PushSubscriptionController` nutzt `@Controller({ path: 'users/me/push-subscriptions', version: '1' })`. Verifikation steht aus, ob `main.ts` `setGlobalPrefix('api')` und `enableVersioning({ type: VersioningType.URI })` sauber konfiguriert sind, sodass die Route tatsächlich als `/api/v1/users/me/push-subscriptions` (oder `/api/users/me/push-subscriptions`, je nach Versionierung) aufgelöst wird. Integrations-Route-Test fehlt; der ADR-011-Claim ist bis dahin unverifiziert.

## Deferred from: code review of story-1.2 (2026-04-21)

- **SW-Handler: Push ohne Body / Payload `text/plain`** [`packages/frontend/public/sw.js:26-36`] — Silent Return bei `!event.data` und bei non-JSON-Payloads. Backend liefert aktuell immer JSON; Keepalive-Pushes sind vom Push-Server-Vertrag nicht vorgesehen. Kein aktives Risiko.
- **Channel-Error-Handling via Substring-Matching** [`packages/frontend/src/features/reminders/services/notification-setup.service.ts:277`] — `errorMessage.includes('not found')` ist projektweites Pattern, auch in Story 1.5 (`createErinnerungChannel`/`createBefehlChannel`). Zentraler Refactor gehört zu einer Plattform-Refactoring-Story.
- **`pushsubscriptionchange`-Event nicht abgefangen** [`packages/frontend/src/shared/ui/push-subscription-manager/push-subscription-manager.tsx`] — Browser kann eine Subscription invalidieren (Server-Key-Rotation, User-Reset). Ohne Handler bleibt der Backend-Eintrag orphaned. Eigene Story in Epic 3 oder Platform-Polish.
- **Permission-Laufzeit-Wechsel (`permissions.query(...).addEventListener('change')`)** [`packages/frontend/src/shared/ui/push-subscription-manager/push-subscription-manager.tsx`] — User aktiviert Permission später manuell; App subscribed erst nach Reload. Opt-in-Verbesserung, kein MVP-Kriterium.
- **`registration.update()`-Trigger in `registerServiceWorker`** [`packages/frontend/src/shared/ui/push-subscription-manager/register-service-worker.ts:26-27`] — Neue SW-Version wird erst nach Tab-Close aktiv. Kopplung an Deployment-Workflow, eigener Scope.
- **`navigator.serviceWorker.ready` Unendlich-Hang** [`packages/frontend/src/shared/ui/push-subscription-manager/useCriticalNotification.ts:60`] — Rare Edge-Case; `registerServiceWorker` läuft in `main.tsx` beim Boot. Timeout/Race ist nice-to-have, nicht MVP-kritisch.
- **Leerer `eventId` via TS-Bypass** [`packages/frontend/src/shared/ui/push-subscription-manager/useCriticalNotification.ts`] — Defensive, TypeScript-Vertrag schützt zur Compile-Zeit.
- **`EventIdLruCache`-Konstruktor akzeptiert `maxEntries <= 0`** [`packages/frontend/src/shared/ui/push-subscription-manager/event-id-lru.ts:19`] — Internal API, Singleton-Nutzung mit fester Konstante `EVENT_ID_LRU_MAX_ENTRIES = 200`.
- **Re-Subscription bei `vapidPublicKey`-Wechsel** [`packages/frontend/src/shared/ui/push-subscription-manager/push-subscription-manager.tsx:97`] — Nur Dev-Only (Hot-Reload); Prod-Env-Key ist stabil zur Deployment-Zeit.
- **`requireInteraction`-Inkonsistenz Web-Foreground (`true`) vs. SW (Default)** [`packages/frontend/src/features/reminders/services/notification.service.ts:453` vs. `packages/frontend/public/sw.js:44`] — Story 1.1 `PushPayload`-Contract kennt kein `priority`-Feld; SW kann nicht entscheiden, ob die Notification persistent bleiben soll. Alignment braucht Contract-Erweiterung in Epic 3.
- **`onclick` löst Vollreload auch bei gleichem Pfad** [`packages/frontend/src/features/reminders/services/notification.service.ts:456-460`] — `window.location.href = url` navigiert immer. Router-Integration (`navigate()`) gehört in Epic 3.
- **SW: `title` nur Whitespace wird nicht getrimmt** [`packages/frontend/public/sw.js:38`] — Backend-DTO erzwingt `@IsNotEmpty`/min(1); Whitespace-Only-Titles kommen vom Contract her nicht an.
- **`icon: '/favicon.ico'` hardkodiert** [`packages/frontend/src/features/reminders/services/notification.service.ts:451`] — Konfigurierbarkeit gehört zur Design-System/Branding-Aufgabe, eigener Scope.

## Deferred from: code review of story-1.3 (2026-04-21)

- **Zirkular-Dep-Risiko mit `KraefteInfrastructureModule`** [`packages/backend/src/modules/auth/auth.module.ts:56`] — AuthModule importiert KraefteInfrastructureModule für den `KRAEFTE_REPOSITORIES.ROLLEN_BESETZUNG`-Token des Guards. Sollte Kräfte-Seite jemals Auth-Funktionalität benötigen, entsteht ein Bootstrap-Zyklus. `forwardRef` wäre die Lösung, aber hypothetisch und aktuell nicht blockierend.
- **DoS-Schutz für riesige `User.permissions`-JSON-Payloads** [`packages/backend/src/modules/auth/guards/einsatz-scope.guard.ts:36-52`] — `parsePermissions` akzeptiert beliebig große JSON-Strings. Byte-Limit wäre defensiv, aktuell keine realistische Angriffs-Oberfläche.
- **TOCTOU zwischen `findUserById` und Repository-Call** [`packages/backend/src/modules/auth/guards/einsatz-scope.guard.ts:124-163`] — User kann zwischen den beiden DB-Calls soft-deleted/gelockt werden. Akzeptabel im MVP; harte Transaktionalität wäre ein eigener Plattform-Scope.
- **TOCTOU zwischen Guard-Check und Handler-Freigabe der Rollenbesetzung** — Rollenbesetzung kann zwischen Guard-Pass und Handler-Ausführung freigegeben werden. Handler sollte bei kritischen Writes eigene Final-Prüfung haben; separate Story.
- **Stammperson-Rebinding zwischen JWT-Ausstellung und Request** — Wenn `User.stammpersonId` während einer Session geändert wird, sieht der Guard neue Rollenbesetzungen. Verhalten ist technisch konsistent, aber nicht explizit dokumentiert.
- **`findUserById` differenziert `deleted` vs. `locked` nicht im Security-Log** [`packages/backend/src/modules/auth/guards/einsatz-scope.guard.ts:126`] — `NotFoundException` wird zu `reason: 'user-not-found'`. Forensisch wäre eine Aufteilung `user-deleted` / `user-locked` hilfreich, erfordert aber auch Differenzierung in `AuthService`.
- **`handlePrismaError` ungetestet** [`packages/backend/src/infrastructure/kraefte/repositories/prisma-rollen-besetzung.repository.ts`] — Preexisting Helper im Repository. Keine neuen Tests für Prisma-Fehler-Code-Mapping. Nicht Scope von Story 1.3.
- **Kein Integrationstest `JwtAuthGuard → EinsatzScopeGuard`** — AC7 verlangt Unit-Tests; die 9 Szenarien sind erfüllt. Ein echter `Test.createTestingModule`-Boot mit beiden Guards in Reihenfolge ist `nice-to-have`, aber erst sinnvoll, wenn die erste Consumer-Route existiert (Story 1.5+).
- **`DEFAULT_EINSATZ_PARAM_NAME` Dreifach-Wahrheit** [`packages/backend/src/modules/auth/guards/einsatz-scope.guard.ts:23, 111`] — Konstante, Fallback-String im Error-Message, ADR-Text. Bei Änderung vergisst man potenziell eine Stelle. Kleiner DRY-Nit.
- **Repo-Spec-Tests (c)/(d) „non-existent einsatzId" / „user-ohne-stamm" sind Tautologien zum Prisma-Mock** [`packages/backend/src/infrastructure/kraefte/repositories/__tests__/prisma-rollen-besetzung.repository.spec.ts:118-135`] — Tests verifizieren nur, dass das Repository die ID an Prisma durchreicht. Spec-Dev-Notes-Kante-Fall-Dokumentation wäre aussagekräftiger, aktueller Test erfüllt aber AC.
- **`tx`-Transaction-Pfad in `findActiveByUserIdAndEinsatzId` nicht getestet** — Port-Signatur nimmt optionales `TransactionContext`, aber kein Test deckt den Transaction-Pfad ab. Aktuell von keinem Consumer genutzt; bei erster Nutzung ergänzen.

## Deferred from: code review of story-1.5 (2026-04-22)

- **Runtime-Schema-Validation für `einsatzContext`-Arrays fehlt** [`packages/backend/src/modules/auth/guards/eigenschutz-rolle.guard.ts:524`, `permissions.guard.ts:637`] — Guards rufen `einsatzContext.einsatzRollenNamen.some(...)` und `einsatzContext.einsatzPermissions.includes(...)` auf ohne Array-Shape-Check zur Laufzeit. TypeScript-Vertrag (`string[]`, non-optional) und `EinsatzScopeGuard` als Plattform-Vertrag sichern das; Defense-in-Depth-Check wäre Plattform-weite Härtung gegen Drift in der Guard-Kette und müsste auch `einsatz-scope.guard.ts` abdecken.
- **Test-Mocks verwenden lose String-Literal-Union statt echten `ValidatedUser`-Typ** [`eigenschutz-rolle.guard.spec.ts:29-32`, `permissions.guard.spec.ts:24-27`] — `MockUser.role: 'ADMIN' | 'SUPER_ADMIN' | 'USER'` matcht aktuell 1:1 mit Prisma-`UserRole`, aber bei Enum-Umbenennung würden Tests grün bleiben und die `=== 'ADMIN'`-String-Checks im Guard in Prod silent fehlschlagen. Direkter Import `import type { UserRole } from '@/generated/prisma/client'` in den Specs wäre Compile-Time-Coupling. Gleiches Pattern in `einsatz-scope.guard.spec.ts` → Plattform-Refactor.

## Deferred from: code review of story-1.4 (2026-04-22)

- **Fehlende DB-FKs auf `einheit_id` durchgängig** [schema.prisma, Eigenschutz-Tabellen] — Architektur-Design (analog Plattform-Pattern `OutboxEvent`, `EtbSnapshot`). Application-Layer-Validierung über Story 1.5 `EigenschutzRolleGuard` + Story 1.3 Membership-Guard. Architektur-Review empfohlen, ob DB-FK in späterer Story nachgezogen werden.
- **Fehlende DB-FKs auf `*_user_id`-Felder** — AC4 explizit: keine Prisma-Relations zu `User` aus Eigenschutz-Tabellen, konsistent mit Bestands-Pattern. Audit-Trail-Integrität aktuell ohne DB-Enforcement.
- **Inkonsistenter Version-Index bei `SicherheitsregelVersion` / `SicherungspostenVersion`** — `GefaehrdungsbeurteilungVersion` hat `@@index([gefBeurteilungId, gueltigVon])`, die anderen beiden Version-Tabellen nicht. Architektur-Inkonsistenz in `architecture.md §1459ff/§1523ff`. Architektur-Fix empfohlen.
- **`Risikoklasse` Enum definiert, aber nicht als Feldtyp verwendet** — Wird vermutlich in Story 2.2 (5x5-Risikomatrix) im `GefaehrdungsbeurteilungVersion.payload` JSONB oder neuem Field referenziert. In Story 2.2-Implementierung verifizieren.
- **`EigenschutzVorfall` hat zwei DateTime-Felder `vorfallZeit` und `wann`** [schema.prisma, Z. ~2806/2808] — Architektur-Quelle deklariert beide Felder ohne semantische Unterscheidung. Redundanz? Doku-Fix oder eins entfernen? Architektur-Klarstellung vor Story 5.x (Vorfallmeldung).
- **`PsaProfilZuweisung`: kein Partial-Unique-Index `WHERE gueltig_bis IS NULL`** — Architektur-konform, aber zwei parallele aktive Zuweisungen pro `(einsatzId, einheitId)` technisch möglich. Für AmpelProjection-Konsistenz ggf. DB-Constraint nachziehen oder Application-Layer-Guard in Story 3.1.
- **`Sicherungsposten.geloescht` Soft-Delete ohne Default-Scope** — Keine DB-Trigger, kein `@@index([einsatzId, geloescht])`. Application-Layer muss überall `geloescht: false` filtern + Performance-Index ergänzen in Story 4.x.
- **`PsaProfilQuittung` ohne FK auf `PsaProfilZuweisung` via `propagationGroupId`** — Architektur-konform (propagationGroup ist Bulk-Operation-Handle, keine Entität). Waisen-Quittungen möglich; Application-Layer-Guard in Story 3.4 nötig.
- **Fehlende FKs auf `einsatzId` bei `AmpelProjection` / `SyncConflict` / `EigenschutzVorfall` / `EigenschutzTelemetryEvent`** — Architektur-Entscheidung (Read-Models/Audit-Logs). Bei Einsatz-Hard-Delete bleiben Zombie-Rows. Design-Review für DSGVO-Löschkette empfohlen.
- **JSON-Felder ohne Schema-Validierung** [`items`, `kontextSnapshot`, `standort`, `personal`, `beteiligte`, `aenderungen`, `payload`] — Application-Layer-Zod-Schemata in Story 2.x+. `kontextSnapshot` sollte Convention `schemaVersion: 'v1'` für Forward-Compat tragen (Story 5.x).
- **Keine CHECK-Constraints** (`version >= 1`, `gueltig_bis > gueltig_von`, `resolved_at` ↔ `resolution`) — Architektur setzt auf Application-Layer-Invarianten. Optionales Hardening in späterer Migration.
- **„Eigenschutz: "-Präfix als String-Literal in `seed.ts`** [`packages/backend/prisma/seed.ts:410–436`] — Story 1.5 soll `EIGENSCHUTZ_ROLLEN_PREFIX`-Konstante einführen; `seed.ts` dann auf Import umstellen (Single Source of Truth).
- **Migration ohne Lock-Timeout / `CREATE INDEX CONCURRENTLY`** — Für Dev/CI unproblematisch. Prod-Deploy: Operative Checkliste mit Wartungsfenster, insbesondere für `ADD CONSTRAINT … FOREIGN KEY` auf `einsaetze`.
- **Seed-Vorlagen ohne Quellen-/Versionierungs-Tracking** — Fachinhalte basieren auf DIN/DGUV/TRBS-Vokabular, aber Source nicht dokumentiert. Governance-Konzept für medizinisch-fachliche Pflege der Vorlagen fehlt (verknüpft mit Decision-Needed: Governance der Seed-Vorlagen).

## Deferred from: code review of story-1.6 (2026-04-22)

- **`KraefteInfrastructureModule` wird in `EigenschutzModule` doppelt importiert** [`packages/backend/src/modules/eigenschutz/eigenschutz.module.ts:29`] — AuthModule importiert das Modul bereits (`auth.module.ts:58`). Dev-Agent-Record dokumentiert empirischen `UnknownDependenciesException` ohne den expliziten Import; trotzdem wäre ein `AuthModule.exports`-Re-Export von `KraefteInfrastructureModule` plattform-ergonomischer (jeder künftige scope-guarded Consumer müsste es sonst lokal wiederholen). Scope der Story hält es bewusst lokal — revisit in Story 2.1, wenn der nächste scope-guarded Controller dazukommt.
- **Route-Test bypass't `Route.useParams()`** [`packages/frontend/src/routes/app/einsatz/$einsatzId/sicherheit/__tests__/eigenschutz.route.spec.tsx`] — `mockUseParams` hängt nicht am tatsächlich konsumierten `Route.useParams()`-Call; funktioniert aber. Änderung der Param-Destrukturierung bleibt unbemerkt. Test-Konvention-Question für die Plattform (Pattern betrifft alle Route-Specs im Projekt).
- **Fehlender Test für `silentError + errorBoundary`-Kombination** [`packages/frontend/src/provider/query-client.provider.test.ts`] — Policy für Zusammenspiel `meta.silentError: true` + globalem Error-Boundary + `throwOnError: false` ist plattformweit underspec'd. Nicht Scope von Story 1.6; Hardening in einer Plattform-Story nachholen, wenn das Pattern breiter angewendet wird.
- **AC10 wörtlicher `NestFactory.create(testModule)`-Bootstrap-Test durch `Reflect.getMetadata`-Checks ersetzt** [`packages/backend/src/modules/eigenschutz/__tests__/eigenschutz.module.spec.ts`] — Begründung: transitiver Modul-Graph (`AuthModule → ServerAccessTokenInfrastructureModule → ServerAccessGuard → EventEmitterModule.forRoot()`) würde 10+ Overrides brauchen. Transparent dokumentiert als Abweichung #4; der echte DI-Regressions-Gate liegt im Controller-Spec via `Test.createTestingModule`. Prinzipielle Lösung (zentraler Test-Helper für Full-Bootstrap) wäre Plattform-Thema.

## Deferred from: code review of story-1.7 (2026-04-22)

- **Type-Cast auf private `eventRegistry`-Feld im Smoke-Test** [`packages/backend/src/infrastructure/outbox/__tests__/eigenschutz-smoke.integration.spec.ts:116`] — `deserializer as unknown as { eventRegistry: typeof deserializerRegistry }` umgeht die Kapselung. Feld existiert aktuell (`event-deserializer.ts:252`), aber Future-Rename produziert einen silent No-op statt Compile-Error. Protected Getter im Deserializer wäre sauber, ändert aber Produktions-Code (out-of-scope). CI würde beim Rename semantisch fehlschlagen.
- **`jest.spyOn(EventSerializer.prototype, 'serialize')` patcht global** [`packages/backend/src/infrastructure/outbox/__tests__/eigenschutz-smoke.integration.spec.ts:97`] — Jest-Worker-Isolation sorgt aktuell für Stabilität, aber instance-level `jest.spyOn(serializer, 'serialize')` wäre eng geführt und konsistent mit „Scope-Grenze"-Narrativ der Story.
- **Adapter-Slug-Prefix-Overlap-Risiko bei zukünftigen Eigenschutz-Event-Namen** [`packages/backend/src/infrastructure/outbox/__tests__/eigenschutz-event-registry.spec.ts:92-104`] — PascalCase-/Slug-Heuristik würde bei Namen wie `vorfall_gemeldet` vs `vorfall_gemeldet_extern` false-positives liefern. Aktuelle 14 Namen haben keinen Overlap. Wenn Epic 2+ weitere Events hinzufügt, muss die Heuristik exakter werden (feste Aspect-Suffixe `WebSocket|Etb|Push|Projection|Telemetry`).
- **Serializer-Override wirft bei parallelen Eigenschutz-Events im Smoke-Test** [`packages/backend/src/infrastructure/outbox/__tests__/eigenschutz-smoke.integration.spec.ts:97-102`] — Sobald Epic 2+ echte Eigenschutz-Events im gleichen Worker emittiert, kracht der Override. Delegation an Original-`serialize` wäre robuster.
- **Keine Runtime-Validierung für leeren `einsatzId`/`userId`/`aggregateId` in `EigenschutzDomainEvent`** [`packages/backend/src/domain/eigenschutz/events/eigenschutz-domain-event.ts:48-58`] — Architecture §D erzwingt Invariant, Basisklasse akzeptiert aber `''`. WebSocket-Room `einsatz:` (leer) oder Outbox-Partition auf `aggregateId=''` wären silent-broken. Validation gehört in konkrete Events oder eigene Domain-Guard-Layer — Epic 2+ Entscheidung.
- **Deserializer-Error-Path (ungültiger Payload) im Smoke-Test ungetestet** [`packages/backend/src/infrastructure/outbox/__tests__/eigenschutz-smoke.integration.spec.ts:117-121`] — Aktueller Test deckt nur Happy-Path. `Result.fail`-Pfad für `einsatzId=undefined` würde Handler-Robustness prüfen. AC5 fordert nur „den" Smoke-Test, nicht Error-Coverage.
- **README referenziert gitignored `_bmad-output/planning-artifacts/architecture.md` mit Zeilennummern** [`packages/backend/src/domain/eigenschutz/events/README.md:7-9`] — Memory dokumentiert `_bmad-output/` als gitignored; Zeilennummern driften mit Edits. Konsistent mit bestehendem Repo-Pattern (andere Story-Dokumente zitieren ebenfalls so), daher als Team-Konvention behandelt.

## Deferred from: code review of story-2.1 (2026-04-22)

- **Backend-DTO validiert via `class-validator` + Regex statt Shared-Zod-Schema** [`packages/backend/src/application/eigenschutz/dto/create-gefaehrdungsbeurteilung.dto.ts`] — Backend-ESM-Migration steht projektweit noch aus, darum kein direkter `@bluelight-hub/shared`-Import im Backend möglich. Die Regex `/^[a-z0-9]{20,32}$/` + Pflicht-/Optional-Matrix spiegelt das Zod-Schema 1:1. Nach ESM-Migration ersetzen durch `@ValidateWithZod(createGefaehrdungsbeurteilungSchema)` — betrifft alle Eigenschutz-DTOs + `complete-setup.dto.ts` als bestehenden Referenzfall.
- **`vitest-axe` nicht installiert — A11y-Assertions rein strukturell** [`packages/frontend/src/features/eigenschutz/ui/organisms/__tests__/GefaehrdungseditorDrawer.organism.spec.tsx` + Schwestern] — Axe-Snapshot-Tests würden eine neue Runtime-Dep einführen. Task 8 nutzt stattdessen strukturelle Assertions (`role="dialog"`, `aria-modal`, `role="radiogroup"`, Headless-UI-Fokus-Trap). Falls projektweit ein A11y-Automation-Gate eingeführt wird, `axe-core` + `@axe-core/react` kanonisch statt `vitest-axe` (Story 7.8 „A11y-Audit" ist der natürliche Anker).

## Deferred from: code review of story-2.2 (2026-04-23)

- **Version-Chain-Timestamp-Ambiguität bei `gueltigBis = gueltigVon`** [`packages/backend/src/infrastructure/eigenschutz/repositories/prisma-gefaehrdungsbeurteilung-version.repository.ts`] — Vorversion `gueltigBis = X` + neue Version `gueltigVon = X` erzeugen bei Range-Queries „WHERE X BETWEEN gueltigVon AND gueltigBis" Matches für beide Versionen im gleichen Moment X. Audit-Queries zur „welche Items galten genau um Uhrzeit X" liefern nicht-deterministische Ergebnisse. Deferred auf Story 2.4 („Vorversionen einsehen") — dort wird die Query-Semantik erstmals konsumiert; dann entweder `gueltigBis = gueltigVon − 1 ms` oder halb-offenes Intervall `[gueltigVon, gueltigBis)` dokumentieren und in der Query nutzen.
- **`title.trim()` Silent-Data-Modification** [`packages/backend/src/domain/eigenschutz/value-objects/gefaehrdung-item.vo.ts`] — Leerzeichen-Trim auf Freitext-Pflichtfeld ist industrie-üblich und erwartbar; keine Audit-/UX-Bedenken. Kein Fix nötig — dokumentiert nur, dass dies bewusst so ist.
- **`null → undefined`-Inkonsistenz Create vs. Update im Frontend-Hook** [`packages/frontend/src/features/eigenschutz/api/queries.ts`] — `useCreateGefaehrdungsbeurteilung` mapped explizit `input.vorlageId ?? undefined`; `useUpdateGefaehrdungsbeurteilungItems` macht das nicht. TanStack-Form + Zod-Schema enforcen die Input-Typen aktuell, ein `null`-Durchreichen ist hypothetisch. Cleanup bei nächster Refactor-Runde, vielleicht in Story 2.5 (Auto-Save) wenn der Hook sowieso angefasst wird.
- **`fs.readFileSync`-Spy-Leak-Risiko in Registry-Konsistenz-Spec** [`packages/backend/src/infrastructure/outbox/__tests__/eigenschutz-event-registry.spec.ts:597-677`] — `jest.restoreAllMocks()` in `afterEach` ist korrekt, aber mehrere `fs.readFileSync`-Spies im selben Test können bei Throws im Zwischenschritt Spy-State zwischen Blöcken leaken. Test-Hygiene, nicht produktions­relevant. Beheben falls Flake beobachtet wird, sonst Story-2.4-Test-Cleanup-Session mit-erledigen.
- **Retry-Policy hart-codiert axios-Error-Shape** [`packages/frontend/src/features/eigenschutz/api/queries.ts:919-921`] — `(error as { response?: { status?: number } } | null)?.response?.status === 403` setzt implizit auf axios-Response-Struktur. Bei HTTP-Client-Wechsel (z. B. zu `ofetch` / native `fetch`) bricht die 403-Erkennung silent und Retry-Storms wären möglich. Spekulativ — aktueller Client ist axios-basiert; in Migrations-Checklisten aufnehmen, nicht proaktiv fixen.

## Deferred from: code review of story-2.3 (2026-04-23)

- **NTP-Drift / `gueltigVon`-Monotonie über Host-Grenzen** [`packages/backend/src/infrastructure/eigenschutz/repositories/prisma-gefaehrdungsbeurteilung-version.repository.ts:~117-120`] — Infrastruktur-Ebene: AC9 stellt per-TX sicher, dass `event.occurredAt` einmalig ist; Cross-Host-Clock-Skew (NTP-Drift zwischen Backend-Instanzen) könnte theoretisch `gueltigBis < gueltigVon` der Vorzeile erzeugen. Nicht Story-2.3-Scope. Fix wäre Server-seitiger Monotonie-Guard beim Chain-Closing (`where: { gueltigVon: { lt: args.gueltigVon } }`) oder Clock-Sync-Garantie auf Infra-Ebene.
- **VO.create-Error-Präfix-Kollision im `wrapValidationError`-Pass-Through** [`packages/backend/src/application/eigenschutz/commands/update-gefaehrdungsbeurteilung-items/update-gefaehrdungsbeurteilung-items.handler.ts:~57-63`] — `wrapValidationError` reicht Strings mit bekanntem Sentinel-Präfix (`ConflictDetected:`, `Invariant:`, `InfrastructureError:`) unverändert durch. Aktuelle VOs liefern keine solchen Präfixe; zukünftige VO-Fehlerklassen könnten durch Mapping-Kollision fälschlich als 409/500 statt 422 mappen. Nur BusinessRule:/NotFound: als Pass-Through-Whitelist härten, sobald eine VO mit kollidierendem Präfix eingeführt wird.
- **AC1-Task-Subtask — `reconstitute()` wirft, statt `Result<Gefaehrdungsbeurteilung>` zu liefern** [`packages/backend/src/domain/eigenschutz/aggregates/gefaehrdungsbeurteilung.aggregate.ts:141-145`] — Task-Subtask in Story 2.3 specifiziert `Result<Gefaehrdungsbeurteilung>`, Dev-Agent lieferte `throw` + Test-Contract auf `toThrow`. AC1-Prosa mandatet den Rückgabetyp nicht; stylistische Abweichung. Aufräumen bei nächster Aggregate-Überarbeitung (vermutlich Story 2.4 oder 2.5, wenn die Rehydrierungs-Pfade für Timeline/Auto-Save angefasst werden).

## Deferred from: code review of story-2.3 — HTTP-Integration-Tests (2026-04-23)

_Follow-up-Entscheidung aus Review 2026-04-23: AC15 mandated „aktive" HTTP-Controller-Integration-Tests für AC2/AC8/AC9/AC12. **AC2 wurde im Review auf Repo-Integration-Ebene aktiviert** (`prisma-gefaehrdungsbeurteilung.repository.spec.ts` — „(Story 2.3 AC2) DB-Level-Lost-Update"). Die drei verbleibenden ACs bleiben HTTP-Level-deferred. Die Repo-/Aggregate-Specs decken die Kern-Invarianten bereits ab._

- **AC8 HTTP-Level Outbox-Retry-Idempotenz** [`gefaehrdungsbeurteilung.controller.integration.spec.ts:99`] — Repo-Level-Test deckt bereits ab: `prisma-gefaehrdungsbeurteilung-version.repository.spec.ts:438` („Unique eventId: zweiter saveNewVersion mit gleicher eventId ist idempotent"). Der HTTP-Level-Test wäre ein Regression-Guard gegen eine regression im Handler-Wiring; würde Auth-Setup + manuellen zweiten Handler-Invoke benötigen. Aktivieren, wenn die `controller.integration.spec.ts`-Test-Infrastruktur (Seeded Admin + JWT + Per-Test-Einsatz) als Helper existiert (Story-2.1-Task 9 / nächster passender Dev-Slot).
- **AC9 HTTP-Level Chain-Intervall-Invariante** [`gefaehrdungsbeurteilung.controller.integration.spec.ts:88`] — Repo-Level-Test deckt bereits ab: `prisma-gefaehrdungsbeurteilung-version.repository.spec.ts:359` („Sequenzielle Updates bauen eine konsistente Chain auf (V1→V2→V3)"). HTTP-Level-Test würde zusätzlich den Handler-`saveNewVersion`-Aufruf + Controller-Pfad abdecken. Selbe Auth-Setup-Voraussetzung wie AC8.
- **AC12 HTTP-Level Remove-All-Flow** [`gefaehrdungsbeurteilung.controller.integration.spec.ts:107`] — Repo-Level-Test deckt ab: `prisma-gefaehrdungsbeurteilung.repository.spec.ts:557` („Happy-Path Remove-All"). Aggregate-Ebene (`gefaehrdungsbeurteilung.aggregate.spec.ts`) deckt Domain-Verhalten ab. HTTP-Level-Test würde Event-Payload (`changedFields.removed = [...]`) via Outbox-Read validieren. Selbe Auth-Setup-Voraussetzung wie AC8/AC9.

## Deferred from: story 2.4 — HTTP-Integration-Test (2026-04-23)

_Task 6.6 der Story schreibt eine Fall A/B/C-Entscheidung vor. **Fall C ist gesetzt:** Das HTTP-Integration-Harness (Seeded Admin + JWT + Per-Test-Einsatz) existiert noch nicht (siehe „Deferred from: code review of story-2.3 — HTTP-Integration-Tests (2026-04-23)" oben für AC8/AC9/AC12). Ein Harness-Build hätte den Story-Scope gesprengt. Die Unit-Tests (Handler-Spec, Controller-Spec) decken die Kern-Logik aktiv ab._

- **AC17 HTTP-Level Integration-Test** [`gefaehrdungsbeurteilung.controller.integration.spec.ts` — neuer `describe('GET …/versionen (Story 2.4)')`-Block mit `it.skip('(Story 2.4 AC17) …')`] — harness-blocked, follow-up mit Story-2.3 AC8/AC9/AC12 (siehe dortigen Eintrag). Der Skip-Skelett enthält bereits die vollständige Assertions-Matrix (DESC-Sortierung, Chain-Intervall V*n.gueltigBis === V*{n+1}.gueltigVon, beide Pfade der Name-Resolution) als Prosa-Kommentar. Aktivierung, sobald das Harness gebaut ist.

## Deferred from: code review of story-2.6 (2026-04-24)

- **AC7 — `PermissionsGuard` + `@RequirePermissions` für Sicherheitsregel-Routen** [`packages/backend/src/modules/eigenschutz/controllers/sicherheitsregel.controller.ts:93`] — PO-Decision 2026-04-24: Permissions-Layer existiert serverseitig (noch) nicht in Bluelight Hub und ist plattformweit out-of-scope für Story 2.6. Eigenschutz-Endpoints sind bisher nur durch `JwtAuthGuard + EinsatzScopeGuard` geschützt; das Permission-Inventar (`architecture.md §B10`) ist Design, nicht Implementation. Wenn Permissions-Layer als Plattform-Story implementiert wird, AC7 gemeinsam mit allen Eigenschutz-Controllern nachziehen.
- **AC8 — WebSocket-Broadcast für `SicherheitsregelAusgerufen`-Event** [`packages/backend/src/infrastructure/eigenschutz/event-adapters/sicherheitsregel-ausgerufen.adapter.ts:42-54`] — PO-Decision 2026-04-24: Adapter bleibt Log-Only; WebSocket-Broadcast wird gemeinsam mit dem ACK-Flow in **Story 2.7** ergänzt (gleicher WS-Room `einsatz:{einsatzId}`, ADR-006). Das TODO im Datei-Kommentar zeigt das bereits an. Story 2.7 muss Adapter-Spec um Broadcast-Assertion erweitern und `WebSocketGateway` injizieren.
- **Pagination-Feld in Liste-Response ohne Implementierung** [`packages/shared/client/models/SicherheitsregelControllerListRegelnVAlpha200Response.ts:8470-8473`] — Generierter API-Client trägt optionales `pagination`-Feld; Controller (`sicherheitsregel.controller.ts`) und Hook (`queries.ts`) füllen/respektieren es nicht. AC6 erwähnt keine Pagination — nicht 2.6-Scope. Cleanup, sobald Pagination-Story für Eigenschutz-Listen kommt.
- **Fanout 100+ Einheiten — Plan-Limit & Performance** [`create-sicherheitsregel.handler.ts:1895-1938`] — Sequentieller Loop mit `await` pro Einheit + Outbox-Insert in einer TX. Bei großen Einsätzen (50+ Einheiten) Lock-Timeout-Risiko. Skalierungs-Story / Plan-Limit (z. B. „max 50 Fanout-Targets"), kein MVP-Blocker.
- **Outbox-Backwards-Compat: alte Payloads ohne `propagationGroupId`** [`infrastructure/outbox/event-deserializer.ts` strict checks] — Deserializer fordert `propagationGroupId`. Kein aktueller Pfad emittiert ohne; Schema-Versioning ist Plattform-übergreifend.
- **`updateWithNewVersion` Wallclock-Skew** [`update-sicherheitsregel.handler.ts:2508`, `prisma-sicherheitsregel-version.repository.ts:5395-5398`] — Domain-Event-Konstruktor setzt `occurredOn = new Date()` im Backend-Prozess, der DB-Commit kommt später. Theoretisch kann V*n+1.gueltigVon < V*n.gueltigVon sein bei Cluster-Skew. Aktuelles Setup ist Single-Node.
- **Adapter-Slug-Proxy als „Registry-Stelle 4"** [`infrastructure/events/adapters/eigenschutz-sicherheitsregel-ausgerufen-event.adapter.ts`] — Re-Export der gleichen Klasse aus zwei Modulen. Konsistenz-Spec zählt es als 4. Stelle. Pattern-Konsistenz mit Gefährdungsbeurteilung; eigene Refactor-Story für 4-Stellen-Registry-Vereinfachung.
- **`saveInitialVersion` ohne expliziten FK-Existence-Check** [`prisma-sicherheitsregel-version.repository.ts:5358-5368`] — FK-Constraint-Verletzung wird als generischer `error.message`-String durchgereicht statt P2003-Spezialfall mit Sentinel. Doppel-Wrapping-Risiko mit `InfrastructureError:`. Defense-in-Depth, im aktuellen Flow nicht reachable.
- **`closeCurrentVersion` `count === 0` Branch als Idempotency** [`prisma-sicherheitsregel-version.repository.ts:5448-5452`] — Verschluckt theoretisch invalide Aufrufe (Caller-Bug), schützt aber Outbox-Retry. Idempotency-by-Design konsistent mit Story 2.3.
- **Conflict-Reload ohne `tx` im In-Place-Pfad** [`update-sicherheitsregel.handler.ts:2511-2522`] — Kommentiert als bewusste Design-Entscheidung (gewollt: aktuellster committeter Zustand für Banner-Text). Edge: parallele TX vs. Bestätigung.
- **`gueltigBis` in der Zukunft fällt aus aktiv-Filter** [`prisma-sicherheitsregel.repository.ts:5583`] — `versionen: { some: { gueltigBis: null } }` matcht keine zukünftigen Werte. Soft-Delete-Pattern-Edge, kein aktueller Pfad setzt das.
- **Char-Count zählt Code-Units, nicht Grapheme (Emojis)** [`SicherheitsregelDrawer.tsx:7170`] — UTF-16 Surrogate-Pairs zählen 2. Bei Emoji-Eingabe wirkt `maxLength=80` strenger. Frontend-Plattform-Story, betrifft auch andere Forms.
- **Re-Wire über Re-Wire Gruppen-Drift bei Outbox-Retention** [`prisma-sicherheitsregel.repository.ts:5713-5740`] — Wenn Original-Create-Event archiviert/gelöscht ist, Fallback `propagationGroupId === regel.id`. Bei langer Lebenszeit + häufigem Re-Wire driftet die Audit-Identität. Lösungsoption: `propagationGroupId` als Schema-Feld (eigene Migration-Story).
- **Re-Wire mit überlappendem Einheit-Set löscht Versionshistorie für gleichbleibende Einheit** [`update-sicherheitsregel.handler.ts:2696-2699`] — Aktuelle Regel für A, neu = [A, B] → Re-Wire deprecated A, legt zwei neue Rows an. Versions-Historie für A startet bei v1 neu. Vermutlich gewollt (jedes Re-Wire ist eine neue Bekanntgabe), Klärung beim PO.
- **Aggregate `update` akzeptiert beliebige `propagationGroupId` ohne Cross-Check** [`update-sicherheitsregel.handler.ts:2497`, `sicherheitsregel.aggregate.ts:3796`] — Kein Cross-Check, dass übergebene Gruppe zur Original-Group passt. Defense-in-Depth, im aktuellen Caller-Flow konsistent.

## Deferred from: code review of story-2.7 (2026-04-27)

- **`ZodValidationPipe` BadRequest-Payload nicht ins Wrapper-Envelope eingebettet** [`packages/backend/src/modules/common/pipes/zod-validation.pipe.ts:3942-3963`] — Cross-cutting Infrastruktur (alle Zod-validierten Endpoints), eigene Plattform-Story.
- **Module-Test für `EigenschutzInfrastructureModule` ↔ `WebsocketModule`-Verdrahtung** [`packages/backend/src/infrastructure/eigenschutz/eigenschutz-infrastructure.module.ts:407-440`] — Generelles Plattform-Test-Härtungs-Item, nicht 2.7-spezifisch.
- **`zuordnungSchema` zwischen Backend-`sicherheitsregel.zod.ts` und Shared-`sicherheitsregel.schema.ts` ohne Konsistenz-Test** — Story-2.6-Stand; Konsistenz-Spec ergänzen, sobald Schema-Drift-Risiko sichtbar wird.
- **`listQuittungen` für nicht existierende Regel liefert `[]` statt 404** [`packages/backend/src/modules/eigenschutz/controllers/sicherheitsregel.controller.ts:4292-4299`] — Aktuell stable empty-state; UX-Detail kann später kommen.
- **`expectedRegelVersion` ohne `@Max`-Cap** [`packages/backend/src/application/eigenschutz/dto/ack-sicherheitsregel.dto.ts:1908-1923`] — Kosmetisch, niedrige Auswirkung.
- **Future-dated `quittiertAm` im Outbox-Deserializer ungeprüft** [`packages/backend/src/infrastructure/outbox/event-deserializer.ts:148-172`] — Cross-cutting (alle Outbox-Events), eigene Hardening-Story.
- **P2002 + concurrent delete → 500 statt idempotent-success** [`packages/backend/src/infrastructure/eigenschutz/repositories/prisma-sicherheitsregel-quittung.repository.ts:2581-2620`] — Sehr niedrige Wahrscheinlichkeit; aktueller Pfad liefert kontrollierte InfrastructureError.
- **Deprecate-Broadcast `einheitId`-Mismatch im Re-Wire-Pfad** [`packages/backend/src/infrastructure/eigenschutz/event-adapters/sicherheitsregel-ausgerufen.adapter.ts:6037-6049`] — Story-2.6-Logik (Re-Wire), nicht 2.7.
- **`einheitId`-Flip-Flop verursacht Socket-Churn** [`packages/frontend/src/features/eigenschutz/api/use-sicherheitsregel-live-banner.ts:107-150`] — UX-Detail, geringes Risiko in der Praxis (Einheit wechselt selten im laufenden Einsatz).

## Deferred from: story-3.1 (PSA-Profil aktivieren / deaktivieren, 2026-04-24)

- **Echter Postgres-Concurrency-Test für `PrismaPsaProfilZuweisungRepository`** — Repo-Spec ist Mock-only. Lost-Update-Branch (`updateMany WHERE version = expectedVersion` → `count === 0`) und AC8-Race (Application-Guard + Postgres-Partial-Unique-Index `psa_profil_zuweisungen_active_unique`) decken die Test-Doubles deterministisch ab, aber kein echter In-DB-Concurrency-Test. Wartet auf den Test-Harness aus **Story 3.9** (Action Item B3 aus Epic-2-Retro). Dann zwei dedizierte Integration-Specs nachziehen: (a) zwei parallele `closeActiveZuweisung`-Aufrufe → einer landet, einer erhält `ConflictDetected:PsaProfilZuweisung:current=<n>`; (b) zwei parallele `saveActivation`-Aufrufe für gleiche Tripel → einer erhält `ConflictDetected:DuplicateActivePsaProfilZuweisung` via P2002 oder Application-Guard.
- **`expectedVersion` Story-DTO-Mismatch zwischen Story-Text und Implementierung** — Story 3.1 ACs/Tasks sahen `expectedVersion: number` auf Command-Ebene vor. Implementierung verwendet `PsaProfilToggleDto.expectedVersion?: number` (per-Toggle). Der Story-Text wurde nicht zurückgespiegelt. Empfehlung: PO-Review klärt, ob die per-Toggle-Variante als verbindlicher Vertrag gilt; Story 3.2 (Bulk) braucht ohnehin per-Toggle und sollte das Pattern fortsetzen.
- **Browser-Smoke ausstehend** — Drawer + Page wurden komponententest-seitig grün geprüft, aber kein End-to-End-Smoke im Chrome (Drawer öffnen, Toggle, Begründung, Submit, Last-BASIS-Modal-Flow, WebSocket-DevTools). Sollte vor dem Code-Review-Pass durchgeführt werden — entweder im Review-Sweep selbst oder in Story 3.3 (kritische Bekanntgabe), wenn der WS-Empfangs-Pfad ohnehin manuell getestet wird.
- **`PSAChangeDrawer` ohne strukturelle a11y-Assertion via vitest-axe** — Konsistent mit den Stories 2.x (vitest-axe ist Repo-weit nicht eingerichtet, siehe ältere Deferred-Einträge). `PSAChangeDrawer.spec.tsx` prüft strukturelle a11y (role="dialog", aria-required, role="alert", role="checkbox"). Volles `vitest-axe`-Setup wartet auf eine eigene Plattform-Story.
- **Telemetrie-Queue als Modul-Level-Array** [`packages/frontend/src/features/eigenschutz/lib/telemetry-queue.ts`] — Story 3.1 AC10 verlangt nur Capture, nicht Upload. Story 3.11 baut den Backend-Endpoint und Flush-Mechanismus; sobald das landet, sollte die Queue-Implementierung auf einen TanStack-Store oder einen IndexedDB-gepufferten Buffer migriert werden, damit Reload-/Tab-Wechsel-Szenarien keine Events verlieren.
- **`useEinsatzEinheiten`-Invalidation als Hardcoded-Key-Tupel** [`packages/frontend/src/features/eigenschutz/api/queries.ts:867`] — `useChangePsaProfil` invalidiert `['kraefte', einsatzId, 'einheiten']` ohne Import von `KRAEFTE_QUERY_KEYS`. Begründung: Cross-Slice-Coupling vermeiden. Wenn der Kräfte-Slice die Key-Struktur ändert, bricht der Refresh stillschweigend. Defense-in-Depth: ein einfacher Konsistenz-Test in `psa-profil-hooks.spec.tsx` könnte den Schlüssel mit `KRAEFTE_QUERY_KEYS.einheiten()` vergleichen — verschoben.

## Deferred from: code review of story-3.1 (2026-04-24)

- **`findByZuweisungId` ohne produktiven Caller** [`packages/backend/src/domain/eigenschutz/repositories/i-psa-profil-zuweisung.repository.ts:2176`] — Methode existiert, aber kein Handler/Controller ruft sie. YAGNI; Story 3.7 (Re-Prompt) könnte sie aktivieren. Bei Aktivierung außerdem `einsatzId`-Filter im Repo (`prisma-psa-profil-zuweisung.repository.ts:2761-2769`) ergänzen, um Cross-Einsatz-Information-Leak zu verhindern.
- **BASIS-Deaktivierung ohne andere aktive Profile triggert kein Last-BASIS-Modal** [`PSAChangeDrawer.tsx:4102-4114`] — Spec-konform (AC5 fordert Modal nur, wenn andere Profile noch aktiv sind). Edge-Hunter wertet das als UX-Lücke: User entfernt einzigen Schutz ohne Bestätigung. UX-Erweiterung Plattform-Härtung.
- **`PsaProfilZuweisung.deactivate` akzeptiert beliebig weit zukünftiges `gueltigBis`** [`psa-profil-zuweisung.aggregate.ts:1962-1986`] — Audit-Trail kann manipuliert werden. Caller-kontrollierter Wert; kein aktiver Risiko-Pfad in Story 3.1, aber Plattform-Härtung empfehlenswert.
- **Roving-Tabindex / ESC-Handling für Chip-Group-Navigation** [`PSAProfileChip.tsx:3742-3748`] — AC2 verlangt nur Space/Enter pro Chip. Pfeiltasten-Navigation und ESC-Schließen wären a11y-Verbesserung; nicht MVP-Blocker.
- **Outbox-Adapter-Slug-Proxy als reine Registry-Stelle 4** [`infrastructure/events/adapters/eigenschutz-psa-profil-geaendert-event.adapter.ts`] — 11-Zeiler-Re-Export, der nur existiert, damit die Event-Registry-Konsistenz-Spec ihn findet. Wächst mit jedem neuen Event mit. Pattern-Refactor in Plattform-Story (bereits in `deferred-work.md` Z. 18 als Story 1.2-Variante notiert).
- **`closeAndCreateNext` ohne P2002-Defense-in-Depth im Closing-Pfad** [`prisma-psa-profil-zuweisung.repository.ts:2781`] — Verbunden mit dem toten Reaktivierungs-Pfad (DEC-4 im Code-Review). Falls der Pfad lebt, sollte der `applyClose`-Schritt P2002-Catch erben.
- **`crypto.randomUUID`-Fallback nutzt `Math.random` für Session-ID** [`packages/frontend/src/features/eigenschutz/lib/telemetry-queue.ts:3696`] — Telemetrie-Session-ID, niedriges Risiko. Comment im Code (`// crypto.randomUUID() ist im Frontend ohne Polyfill verfügbar`) widerspricht sich selbst — Comment in P-Patches korrigieren oder beim Story-3.11-Refactor entfernen.
- **`eigenschutzTelemetryQueue.drain()` als public API ohne Test-Marker** [`telemetry-queue.ts:3680`] — Story 3.11 wird die Methode als Flush-Trigger nutzen. Kein Schutz vor accidental-drain durch Drittcode; aktuell harmlos.
- **`Invariant:*`-Mapping unerreichbar im Controller** [`psa-profil.controller.ts:3342`] — `INVARIANT_UPDATE_COUNT_ANOMALY` aus dem Repo trifft `where: { id }`-UNIQUE-Pfad → physisch nicht erreichbar. Defensive-Code; harmlos.
- **`Math.max(...arr)` Stack-Risiko bei sehr großen Arrays** [`queries.ts:656`] — Max 5 Toggles via DTO. Theoretisches Issue.
- **`IPsaProfilZuweisungReadRepository.tx?` ungenutzt** [`i-psa-profil-zuweisung.repository.ts:2225`] — Speculative API. Kann beim ersten produktiven Aufrufer entfernt oder verkabelt werden.
- **Telemetry-Union enthält Events ohne Emitter** [`telemetry-queue.ts:3648`] — `assess_completed`, `assess_aborted`, `cbrn_acknowledged` werden im Type-Union deklariert, aber Story 3.1 emittiert nur `assess_started`. Story 3.11/3.3 emittieren das.
- **Backend-DTO CUID-Format-Drift** [`change-psa-profil.dto.ts:1278`] — Backend validiert IDs nur mit `@IsString()`, Shared-Zod nutzt `cuidIdSchema`. A2 ESM-Carry-Over; Plattform-Story löst das.

## Deferred from: code review of 415-3-2 (2026-04-24)

- **N×Refetch im `fetchAffectedBulk` sequenziell** [`psa-profil.controller.ts:1540-1552`] — bei Cap=50 ≈1 s zusätzliche Latenz (TX bereits committed, nur Response verzögert). Mitigation: dedizierter Bulk-Read-Endpoint `psaProfilControllerGetPsaProfilesByEinheitenVAlpha` + Response-Hydration aus einem Call. Bekannt durch Story-3.2 Open-Items.
- **`useReducedMotion` Initial-State-Race** [`use-reduced-motion.ts:2347-2355`] — zwischen lazy `useState`-Init und `addEventListener`-Setup wechselt OS-Setting → veralteter Initial-State. Theoretisch, niedriges Risiko.
- **`attemptedVersion` im 409-Body verloren bei mixed `expectedVersion` (Bulk)** [`queries.ts:2076-2079`] — UX-Regression vs. Single-Pfad: bei N>1 mit unterschiedlichen Versionen ist `attempted = undefined`, Banner kann „Version X versucht" nicht zeigen. Abhängig von Decision zur per-Einheit-Version-Map (siehe Story-3.2 Review-Decision #1).
- **`findActiveByEinheit` läuft N×M-mal in einer TX** [`change-psa-profil.handler.ts:622-636`] — 50 Einheiten × 5 Toggles = 250 sequenzielle Reads in einer TX, hält Locks lange. Bei realistischem MVP-Cap (10–20) unkritisch, bei Defense-Cap problematisch. Mitigation: pre-fetch `findActiveByEinheitenIds(einsatzId, einheitIds, profilSet)` in einem Read.
- **Empty-Catch im `Teilnehmer`-Lookup-Pfad** [`change-psa-profil.handler.ts:604-609`] — Pre-Story-3.1-Pattern. Verschluckt Stack-Trace, Logger wird nicht aufgerufen. Konsistenz-Refactor sinnvoll, Plattform-Härtung.
- **OpenAPI-Bulk-`@ApiNotFoundResponse` ohne Einheit-Annotation** [`psa-profil.controller.ts:1516`] — bewusst per Spec/Test (NotFound-Sentinel rollt TX zurück, keine Einheit-Annotation). Frontend kann Einheit-spezifische 404-Banner-Texte nicht zeigen. Akzeptiert für Story 3.2; UX-Erweiterung möglich.
- **AC9 reale 401/403-HTTP-Pfade `.skip`** — wartet auf Story-3.9-Test-Harness (B3 Action Item). Strukturell durch Reflect-Metadata + Single-Endpoint-Tests gedeckt.

## Deferred from: code review of 415-3-2 — second pass (2026-04-24)

- **`psa-profil-hooks.spec.tsx` mockt nur Single-API, nicht Bulk** [`packages/frontend/src/features/eigenschutz/api/__tests__/psa-profil-hooks.spec.tsx`] — `useChangePsaProfil`-Hook routet seit Story 3.2 bei `einheitIds.length > 1` auf den Bulk-Endpoint, der bestehende Spec-Mock deckt diesen Pfad nicht. Mitigation: Mock-Erweiterung `psaProfilControllerBulkChangePsaProfilVAlpha` + dedizierter Test, der Bulk-Routing verifiziert. Strukturell durch Drawer-Spec-Bulk-Tests bereits abgedeckt.
- **Orange-Fade-Indikator-Snapshot mit Response-DTO als Quelle** [`PsaProfilePage.tsx`] — aktueller Implementierungspfad nutzt einen Local-State-`Set<einheitId>` mit 600 ms Auto-Clear. Snapshot-Test gegen das Response-DTO würde den Pfad gegen Refactors absichern, erfordert aber strukturellen Refactor (Response-DTO durch alle Layers reichen). Niedrige Priorität — Verhalten ist durch `recentlyMutated`-Spec abgedeckt.
- **`recentlyMutated`-Cleanup-Merge-Bug** [`PsaProfilePage.tsx:3897-3920`] — beim raschen Folge-Submit innerhalb der 600 ms kann der Auto-Clear-Timeout der ersten Operation einen Teil der zweiten Selection clearen, wenn beide Sets überlappen. Edge-Case mit minimaler User-Visibility (Fade endet einen Tick zu früh). Mitigation: Map<einheitId, timeoutId> statt Set + per-einheitId-Cleanup.
- **`useQueries`-Fanout im Bulk-Drawer (N parallele GETs)** [`PSAChangeDrawer.tsx`] — bei N=50 (Defense-Cap) feuern 50 parallele `psaProfilControllerGetPsaProfileVAlpha`-Calls. Im realistischen MVP-Cap (10–20) unkritisch, bei höheren N wäre ein dedizierter Bulk-Read-Endpoint sinnvoll (gleiche Mitigation wie `fetchAffectedBulk`-Sequenz oben).

## Deferred from: code review of 415-3-2 — third pass (2026-04-24)

- **Sentinel-Annotation-Suffix-Injection (Defense-in-Depth)** [`change-psa-profil.handler.ts:141-146`, `psa-profil.controller.ts:2045-2054`] — DTO-Validierung enforced `@IsCuid2` für `einheitIds`, daher heute kein aktiver Bug. Defense-in-Depth durch strukturierte JSON-Annotation-Payload statt Sentinel-String wäre sauberer; lohnt erst, wenn Annotation-Surface wächst.
- **`closeAndCreateNext` toter Code im Repo-Interface** [`change-psa-profil.handler.ts:302-388`, Repo-Interface] — Methode ist im Test-Stub vorhanden, wird aber nirgends aufgerufen. JSDoc spricht von „Wechsel A→B"-Pfad, der im Handler so nicht modelliert ist. Repo-Cleanup-Story.
- **N+1 + 250 sequenzielle Repo-Calls in einer 10s-TX** [`change-psa-profil.handler.ts:222-255`, `transactional-command.handler.ts:286-291`] — Worst-Case (Cap=50, M=5) führt zu 300 round-trips; jeder Lock-Spike kann die TX killen. Mitigation: `findByIds`-Batch-API + Pre-Fetch `findActiveByEinheitenIds(einsatzId, einheitIds, profilSet)`.
- **TOCTOU Cross-Einsatz-Membership-Check** [`change-psa-profil.handler.ts:222-231`] — Zwischen `findById` und `saveActivation` kann eine andere TX die Einheit verschieben. Schutz erfordert serialisierbare Isolation oder FK auf `(einsatzId, einheitId)` für `PsaProfilZuweisung`. Plattform-Defense-Story.
- **`wrapInfrastructureError` leakt SQL-/DB-interne Strings** [`change-psa-profil.handler.ts:118-124, 209`] — Originale Repository-Error-Messages landen unverändert im 500er-Body. Tabellen-/Spalten-Namen/Constraint-Namen könnten leaken. Zentraler Filter-Refactor erforderlich (alle Repos betroffen).
- **`useLongPress.justFiredRef` ohne TTL** [`use-long-press.ts:2874-2880`] — `pointercancel` ohne nachfolgenden `click` (z. B. nach Scroll) lässt das Flag bis zum nächsten beliebigen Click stehen. Edge-Case auf manchen Mobil-Browsern.
- **`prefers-reduced-motion` greift nicht auf `transition-colors`** [`PsaProfilePage.tsx:3422`] — Karte hat `transition-colors` ohne `motion-reduce:transition-none`-Prefix; reduced-motion gilt nur für den 600 ms Fade. Visual nit.
- **`crypto.randomUUID()`-Fallback Kollisions-Risiko** [`PSAChangeDrawer.tsx:3592`] — `Date.now()-Math.random().toString(36)` kann in Browsern ohne `crypto.randomUUID` bei zwei Drawer-Opens innerhalb 1 ms identische IDs liefern. Telemetry-only.
- **`telemetryRefs.current` userId-Switch im Effect nicht getriggert** [`PSAChangeDrawer.tsx:3585-3601`] — Login-Switch im Multi-User-Tab während offenem Drawer wird vom `[open]`-Effect nicht erfasst. Rare.
- **Long-Press startet bei Klick auf inneren Button** [`PsaProfilePage.tsx:3393-3414`] — Card-Click-Handler filtert via `closest('button')` früh, aber `onPointerDown` auf der `<article>` feuert auch bei Button-Tipp. UX-Polish: `event.target.closest('button')` auch in `onPointerDown`.
- **`duration-[600ms]` ohne stabile `transition`-Class** [`PsaProfilePage.tsx:3422`] — `duration-[600ms]` wird nur dynamisch gesetzt; bei Class-Removal entfällt das Easing → harter Snap statt Fade-out. Visual nit.
- **Drawer-Tests mocken `useChangePsaProfil`-Hook statt API** [`PSAChangeDrawer.spec.tsx:4040-4068`] — Mock-Strategie bypassed Bulk-vs-Single-Routing-Logik im Hook; wenn jemand den Hook ändert (z. B. Single statt Bulk bei N=1), bleiben Tests grün. End-to-End-Mock-Pfad sinnvoll.
- **`staleTime: 15_000` + `invalidateQueries` ist nicht sofort** [`PSAChangeDrawer.tsx:3537, 3554`] — `invalidate` setzt nur stale-Marker, nicht den nächsten Fetch. UX-Polish im Conflict-Refetch-Pfad.
- **`mapErrorToInlineText` hardcoded deutsche Strings ohne i18n-Zentralisierung** [`PSAChangeDrawer.tsx:3979-3997`] — Drift-Risiko zwischen Banner-JSX und Mapper. Plattform-i18n-Story.
- **`submitDisabled` prüft nicht `bulkQueries.isLoading`** [`PSAChangeDrawer.tsx:3654-3656`] — Submit auf leerem Server-State möglich, wenn Toggles + Begründung gesetzt sind, aber Reads noch laufen. Verwandt mit dem Patch zu E12 (failende Bulk-Queries).
- **N=50-Boundary-Test ohne Outbox-Save-Verify** [`change-psa-profil.handler.spec.ts:518-525`] — Test prüft nur `result.isSuccess`, nicht alle 50 Outbox-Events mit identischer `propagationGroupId`.
- **Bulk-Reihenfolge bei Multi-Toggle (N=3, M=2) ungeprüft** [`change-psa-profil.handler.spec.ts:770-788`] — `affectedZuweisungen`-Reihenfolge nicht assertiert; Refactor mit Loop-Tausch würde Tests grün lassen.
- **DTO-Validation-Tests fehlen** [`change-psa-profil.dto.ts`] — kein `dto.spec.ts` für CUID2/ArrayUnique/Trim-Verhalten/`@MinLength(1)`. Class-validator-Decorators sind ungetestet.
- **Controller-Test mockt alle 3 Guards strukturell** [`psa-profil.controller.spec.ts:2095-2102`] — Reihenfolge wird via Reflect-Metadata geprüft; Laufzeit-Verhalten („`PermissionsGuard` liest `request.einsatzContext`") nicht abgedeckt. Wartet auf Story-3.9-Test-Harness.

## Plattform-Item 2026-04-24: Idempotency-Keys für Mutationen (aus Story-3.2 Review)

Bei Mid-Bulk-Network-Loss erzeugt der client-seitige Retry (`queries.ts` `useChangePsaProfil`) eine zweite `propagationGroupId` und einen doppelten Audit-Trail-Eintrag — gleiche Logik betrifft alle anderen Eigenschutz-/Sicherheits-Mutationen (Sicherheitsregeln, Gefährdungsbeurteilung, ETB) und gehört architektonisch in eine eigene Plattform-Story.

**Optionen für Folge-Story:**

- **(a) Idempotency-Key-Header:** Frontend generiert pro Submit eine UUID (`Idempotency-Key`-Header), Backend dedupliziert via Outbox/Cache mit z. B. 60s-TTL. Standard-REST-Pattern, gut testbar.
- **(b) Frontend-Mutation-ID + Server-Side-Dedup-Cache:** Client generiert UUID im Mutation-Body, Server-Side hält 60s-Cache mit (userId, mutationId, hash) → bei Duplicate-Request liefert er den gecachten Response. Erfordert Cache-Layer im Backend.
- **(c) Bewusst ignorieren:** Audit-Trail-Doppel ist akzeptabel, Sicherheitsbeauftragter erkennt's im Verlauf. Niedrigster Aufwand, höchstes Risiko.

**Empfehlung Plattform-Pattern:** (a) Idempotency-Key-Header — Standard, testbar, erweiterbar. Sollte vor Live-Deployment der Eigenschutz-Module entschieden werden.

## Refactor 2026-04-28: Eigenschutz-Rollen-Schicht entfernt

- Domain-Enum `EigenschutzRolle`, Decorator `@RequiresEigenschutzRolle`, Guard `EigenschutzRolleGuard` und alle zugehörigen Specs/Provider entfernt.
- Permissions-Guard ist die einzige verbleibende Autorisierungsschicht für Eigenschutz-Endpoints.
- Begründung: doppelte Autorisierung (Rolle + Permission) erzeugte Pflege-Aufwand und User-facing-Verwirrung beim 403-Mapping; Permissions sind die Source of Truth.
- Folge-Arbeit: PRD/Architecture-Docs und ADRs prüfen, ob "Vier-Schicht-Kette" noch irgendwo als Plattform-Pattern propagiert wird (sollte sie nicht mehr sein).

## Deferred from: code review of 415-3-6-rueckmeldung-ausruestungs-luecke-an-sicherheitsbeauftragten (2026-04-24)

- **Auth-Helper unter „loose-repo"-Szenario untestet** [`caller-authorization.ts:228-232`] — Defense-in-Depth-Branch (`teilnehmer.einsatzId !== params.einsatzId`) ist nur unit-getestet, kein Integration-Fixture mit lockerem Repo. Pre-existing pattern aus AckPsaQuittungHandler übernommen.
- **`lookupPsaPropagationExists` silent fallback** [`lookup-psa-propagation.ts:272-280`] — Test-Mock-Fallback (`if (!client.outboxEvent) return false`) maskiert produktionsseitige Tx-Context-Bugs. Pre-existing aus Story 3.4 Helper-Extraktion.
- **DTO `einheitId`-Regex `^[a-z0-9]{20,32}$` vs. cuid2-Realität** [`melde-luecke.dto.ts:836`] — Regex erlaubt 20-32 lowercase alphanum; cuid2-Default ist 24, aber kein Edge-Test. Pre-existing pattern, projektweit konsistent (AckPsaQuittungDto identisch).
- **`wrapInfrastructureError` matched per String-Prefix** [`melde-luecke.handler.ts:799-801`] — Sentinel-Erkennung via `BusinessRule:`-Prefix-Match ist fragil; Repository-Fehler mit „BusinessRule" als Substring könnte durchschlagen. Pre-existing utility, projektweite Konvention.
- **`OffenePsaBekanntgabeEntryDto.lueckenCount` FE-Manuell vs. Generated Drift** [`queries.ts:2517-2519`] — Hand-maintained Frontend-Interface markiert Feld optional, generierter Client + Backend-DTO required. Cleanup-Pass für hand-maintained Interfaces nötig.
- **Controller-Spec ohne 400-Sentinel-Mapping-Test** [`psa-profil.controller.luecke.spec.ts`] — Spec AC16 listet 400; im Unit-Test-Scope debattierbar (Pipe/DTO-Layer testet Validation separat).
- **Concurrent `meldeLuecke` `created`-Telemetrie-Race** [`prisma-psa-profil-quittung.repository.ts:1592-1598`] — `findUnique` vor `upsert` macht `created`-Flag race-anfällig. Telemetrie-Genauigkeit, akzeptabel für MVP. Story 6.x kann atomares CTE liefern.
- **Outbox-Retention purged `psa_profil_geaendert` vor später Lücke-Meldung** [`lookup-psa-propagation.ts:272-289`] — Wenn Retention die Source-Outbox-Row entfernt, lehnt der Handler legitim spät kommende Meldungen mit `NotFound:PsaPropagation` ab. Architektur-Frage zur Retention-Strategie, Story 6.x.
- **Server-Clock-Drift `gemeldetAm < quittiertAm`-Inversion** [`melde-luecke.handler.ts:807-808`] — Handler nutzt `new Date()` (Q3-Default); bei Clock-Skew kann der Event-Zeitstempel vor einer bereits persistierten Quittung liegen. Audit-Timeline tolerant; Drift selten.
- **Path-Param-Validation für `einsatzId`/`propagationGroupId` am Controller** [`psa-profil.controller.ts:2162-2170`] — Malformed Cuid2 erreicht Handler und produziert 404/500 statt 400. Pre-existing Repo-Konvention (alle Controller); eigener Cleanup-Pass.

### Promoted aus Decision-Needed (2026-04-24, Story 3.6)

- **Receiver-Bestätigung „bereits Lücke gemeldet"** [`PsaProfilEmpfangBanner.tsx`, `PsaProfilDetailDrawer.tsx`] — Nach Submit kein UI-Hinweis, dass die Gruppe bereits gemeldet wurde. Reason: MVP-organisch; Empfänger-Drawer Story 6.x liefert dauerhaftere Sichtbarkeit.
- **WS-Payload-PII-Konsistenz `einheitId` raw vs. `userIdHash`** [`luecke-gemeldet.adapter.ts`] — Inkonsistente Redaction. Reason: projektweite Konvention bestehender WS-Adapter (Story 3.4 etc.); eigener Cleanup-Pass über alle Adapter, nicht Story 3.6 isoliert.
- **DB-Index für Outbox-JSON-Path-Lookup** [`lookup-psa-propagation.ts:282-289`] — Linearer Scan auf `outbox_event` per JSON-path-Equality, jetzt für Quittung UND Lücke genutzt. Reason: bei aktueller Last (≤ 100 Events/Group, Retention) ausreichend; Plattform-Performance-Pass oder Story 6.x.
- **`status='partial'` überlagert Bedeutung** [`list-offene-psa-bekanntgaben.handler.ts:1041-1044`] — `partial` heißt jetzt sowohl „nicht vollständig acked" als auch „acked + Lücken". Reason: Story 6.x `AmpelProjection` ist die richtige Heimat für ein explizites Statussystem; jetzt ein DTO-Feld zu erfinden würde Story 6.x präjudizieren.
- **Lücken-Notiz-Überschreibung durch Einheits-Mitglied** [`prisma-psa-profil-quittung.repository.ts:1611-1614`] — User B kann Notiz von User A überschreiben ohne Audit-Trail der Ersetzung. Reason: aktuelle MVP-Semantik ist „Einheit teilt Notiz" (analog zur Quittung); fremde Notiz zu überschreiben ist akzeptiert, Audit-Trail-Pflege Phase 2.
- **Forward-compat Invalidierungs-Key `lueckeStatusByGroup`** [`queries.ts:useMeldeLuecke`] — `useMeldeLuecke` invalidiert noch keinen `lueckeStatusByGroup`-Key, weil die Story-6.x-`AmpelProjection`-Query selbst noch nicht existiert. Reason: invalidieren einer nicht-existenten Query wäre premature; Forward-Compat-Wiring zusammen mit der Query in Story 6.x liefern.

## Deferred from: code review of 415-3-7-re-prompt-scheduler-eskalation-nach-5-min-ohne-quittung (2026-04-24)

- **`originalEventId` ohne FK zu Outbox-Row** [`quittung-ueberfaellig.event.ts:48`] — Wenn Outbox-Pruning die Original-Row entfernt, läuft der Audit-Drill-Down ins Leere. Pre-existing Outbox-Pattern; Retention-Strategie ist Plattform-Frage.
- **`zuweisungId` ohne CUID-Validation im Event-Schema** [`quittung-ueberfaellig.event.ts:75-80`] — Backend-Konstruktor übergibt `zuweisungId` ohne Format-Check; ein malformiertes Outbox-Payload (z.B. `""`) würde durchschlagen. Hardening für Phase 2, MVP nicht kritisch (eigene Erzeuger-Pfade sind kontrolliert).
- **`publisher.broadcast()` ohne Timeout** [`quittung-ueberfaellig.adapter.ts:539-549`] — `await publisher.broadcast(...)` kann theoretisch hängen. Infrastructure-Layer-übergreifend; alle Eigenschutz-Adapter sind betroffen.
- **Concurrent-Property-Test für Race Step1a/1b/3 im Handler** [`emit-psa-quittung-ueberfaellig.handler.ts:444-477`] — Defense-in-Depth-Re-Check ist nicht concurrent-getestet. Sinnvoll erst nach Decision D1 (Multi-Pod-Strategie); bei Single-Pod-MVP unnötig.
- **`PsaProfilGeaendert`-Payload missing-`einsatzId`-Guard im SQL** [`prisma-psa-propagation-overdue-query.repository.ts:728`] — Falls Outbox-Payload `einsatzId` fehlt, wird `null` propagiert und Command failt cryptisch. Story-1.7-Schema-Concern; alle Eigenschutz-Events betreffend.
- **NULL-Semantik in NOT EXISTS für nullable q-Spalten** [`prisma-psa-propagation-overdue-query.repository.ts:735-737`] — `psa_profil_quittungen.einheit_id`/`propagation_group_id` sind nullable seit Story 1.4. SQL-NULL-Semantik in NOT-EXISTS-Subqueries kann false-match erzeugen. Schema-Hardening (NOT NULL constraint) eigener Pass.

### Promoted aus Patches (2026-04-24, Story 3.7 Code-Review-Resolution)

- **D1 — Multi-Pod-Scheduler-Race + DB-Idempotenz** [`reprompt-psa-quittung.scheduler.ts`, `emit-psa-quittung-ueberfaellig.handler.ts`] — Single-Pod-`this.running`-Guard in 3.7 implementiert. Multi-Pod-Hardening (Postgres-Advisory-Lock im Scheduler oder Handler-Tx, alternativ Unique-Constraint auf `outbox_events(eventName, payload->>'propagationGroupId', payload->>'einheitId')`) erfordert layer-sensiblen Refactor (Scheduler benötigt PrismaService-Zugriff oder neuen Lock-Port; Handler-Tx-Konflikte vermeiden). Eigene Story sobald Multi-Pod-Deployment ansteht. Reason: MVP läuft single-pod; Korrektheits-Garantie für Pod-Race ist explizit deferred.
- **D2 — JSONB-Path-Performance ohne Functional Index** [`prisma-psa-propagation-overdue-query.repository.ts`] — Scheduler-Query macht JSONB-Path-Extracts in Outer + 2 NOT-EXISTS-Subqueries, ohne functional index linear in Outbox-Größe. Spec-Q4 verbietet Migration in Story 3.7. Auf Story 6.x (AmpelProjection) verschoben — dort kommt eine dezidierte Index-Migration für mehrere JSON-Path-Lookups gleichzeitig. Reason: MVP-Last (≤ Hunderte Events/Tag, Outbox-Retention) macht Full-Scan tolerabel; isolierte Index-Migration in 3.7 wäre verfrüht.
- **P2 — Repository-Spec mock-only — AC2-Pflicht-Cases ungetestet** [`prisma-psa-propagation-overdue-query.repository.spec.ts`] — Mock-Spec deckt das Result-Mapping; AC2-Pflicht-Cases (AKTIVIERT/DEAKTIVIERT-Pärchen → 1 Zeile, NOT-EXISTS-Idempotenz, Threshold-Grenzfall `<` strikt, Limit-Cap stabile ASC-Reihenfolge) sind nicht behavioural-getestet. Ein Prisma-Test-Helpers-Integration-Spec (Pattern: `outbox-race-condition.integration.spec.ts`) wäre die richtige Heimat — eigener Test-Infrastructure-Pass, nicht in 3.7.
- **P7 — Threshold im JS-Tick statt im SQL** [`reprompt-psa-quittung.scheduler.ts`, `prisma-psa-propagation-overdue-query.repository.ts`] — Pod-Clock-Drift ist theoretisch ein Korrektheitsthema, praktisch low-impact (NTP-Sub-Sekunden-Drift verschiebt einen 5-min-Threshold nicht relevant). Saubere Fix erfordert Port-Vertrags-Refactor (Date → thresholdMinutes:number, ueberfaelligSeitMin als SQL-Spalte). Zusammen mit D1-Multi-Pod-Hardening lösen.
- **P8 — Lookup-Helper schlucken undefined Delegate als `false`** [`lookup-psa-quittung-abgegeben-for-group.ts`, `lookup-psa-quittung-ueberfaellig-emitted.ts`] — Identisches Test-Mock-Fallback-Pattern in `lookup-psa-propagation.ts` + weiteren Eigenschutz-Helpern (Story 3.4, 3.6 — vgl. deferred-work.md Eintrag „loose-repo-Szenario untestet"). Isolierter Fix in 3.7 wäre projektweit inkonsistent; eigener Plattform-Pass alle Helper gleichzeitig hardenen.
- **P12-Rest — UI-Feedback für `status === 'error'` im Live-Hook** [`use-eigenschutz-quittung-ueberfaellig-live.ts` Konsumenten] — Doppel-Increment-Guard ist gefixt; was fehlt ist eine UI-Komponente, die `status === 'error'` als „Verbindung verloren" darstellt mit manuellem Reload-Button. Konsumenten-UI-Refactor (Banner-Wrapper, EinsatzleiterReprompEskalationBanner), eigene Story für WS-Connection-Failure-UX-Pattern.

## Deferred from: code review of 415-3-8-push-notifications-fuer-kritische-psa-events (2026-04-29)

- **W1 — Pod-Restart Re-Dispatch / Outbox-At-Least-Once nicht E2E getestet** [`emit-critical-push-on-psa-profil-geaendert.handler.ts` Klassen-Doc] — Comment behauptet, der Outbox-Dispatcher feuert beide `@OnEvent`-Handler (WS + Push) nach Restart erneut; kein E2E-Test verifiziert WS+Push-Dual-Re-Emit + Client-LRU-Dedup im Restart-Szenario. Outbox-Story-Scope, betrifft alle Outbox-`@OnEvent`-Handler — eigener E2E-Test-Pass.
- **W2 — `@OnEvent` per String statt Class-Reference** [`emit-critical-push-on-psa-profil-geaendert.handler.ts:@OnEvent`] — `@OnEvent(PsaProfilGeaendertEvent.eventName())` resolvt class-load-time; bei Rename könnten WS- und Push-Handler unabhängig drift'en ohne Compile-Fehler. NestJS-event-emitter-API-Constraint, projektweit; Hardening über alle bestehenden `@OnEvent`-Adapter wäre eigener Plattform-Pass.
- **W3 — Stale Recipients während gleichzeitiger Membership-Updates** [`prisma-push-recipient-lookup.repository.ts` $queryRaw außerhalb Tx] — Wenn `EinsatzPersonEinheit`-Mitgliedschaft im selben Moment gerade gemerged wird, liefert Lookup pre-change-State und neuer Joiner verpasst CBRN-Alert. Existing transactional pattern; projektweite Tx-Strategie für Read-Side-Lookups eigener Hardening-Pass.
- **W4 — Sync-DB-Lookup im Writer-Tx-Kontext** [`emit-critical-push-on-psa-profil-geaendert.handler.ts` `@OnEvent` ohne `{ async: true }`] — Wenn `@OnEvent` synchron aus Writer-Tx feuert, läuft Lookup innerhalb der Tx-Boundary → Tx-Verlängerung/Deadlock-Risiko. CQRS-Stack feuert Outbox-Events außerhalb der Writer-Tx; Verifikation über alle Adapter-Pfade ist eigener Audit-Pass.
- **W5 — Hardcoded German Body-String ohne i18n-Hook** [`psa-profil-geaendert.mapper.ts` Body-Konstante] — „Schutzstand der Einheit hat sich geändert. Tippen für Details." hardcoded, kein `t(...)`-Wrapper. Spec-Q4-Default markiert i18n explizit als Phase-2; zusammen mit globalem Push-i18n-Layer angehen.
- **W6 — `trimToCap` cap=1-Boundary nicht getestet** [`psa-profil-geaendert.mapper.ts:trimToCap`] — Bei `cap=1` würde `slice(0, 0) + '…'` = nur Ellipsis ergeben. Aktueller Caller hits this nicht; defensiver Refactor zusammen mit P5 (Byte-Cap-Migration) sinnvoller — getrennt einzubauen wäre lokaler Aufwand ohne Mehrwert.
- **W7 — `event.profil` null-Guard im Mapper** [`psa-profil-geaendert.mapper.ts:profilLabel`] — Bei null/undefined-`profil` ergibt Title „PSA-Profil undefined aktiviert". TS-Typ verbietet null im `PsaProfilGeaendertEvent`-Constructor; Domain-Schicht garantiert Wert. Boundary-Test wäre Belt-and-Suspenders.
- **W8 — SQL-Input ohne CUID-Length-Validation** [`prisma-push-recipient-lookup.repository.ts:listRecipientsForEinheit` Input] — Pathologically large IDs (Multi-MB) werden an Postgres durchgereicht; Connection-Memory-Risiko. CUID-Format-Check via `@IsCuid()` an Controller-Boundary fängt das in Production ab; Repo-seitige Validation redundant.

### Promoted aus Decisions (2026-04-29, Story 3.8 Code-Review-Resolution)

- **D1 — Unbounded Push-Fanout ohne Concurrency-Cap / Per-Send-Timeout** [`emit-critical-push-on-psa-profil-geaendert.handler.ts` `Promise.allSettled`] — Alle `push.send`-Calls starten simultan, ohne Limit/Timeout. Bei großer Einheit (≥ 50 Personen × n Geräte): Provider-Rate-Limit-Risiko, Event-Loop-Last; einzelner blackholed Endpoint kann den Outbox-Dispatcher blockieren. Reason: MVP-Pilotvolumen niedrig; Hardening (`p-limit`-Chunking + 5-s-Per-Send-Timeout via `Promise.race` + `push_blackhole`-Counter im Log) zusammen mit Story 3.11 Telemetrie-`push_delivered` und Provider-Rate-Limit-Tracking bündeln (analog 3.7-D1-Multi-Pod-Hardening-Pattern).
- **D4 — Repository-Spec mock-only (kein Real-DB-Roundtrip-Test)** [`prisma-push-recipient-lookup.repository.spec.ts`] — Spec/Task-Widerspruch: AC2 fordert echte Prisma-Test-Helpers, T2.6 fordert Mock. Implementation folgt T2.6. SQL-Korrektheit (snake_case-Mapping, `stammpersonId`-Casing) ist via Schema-Inspektion + Mock-Pipeline-Verifikation gesichert; Roundtrip-Lücke bleibt. Reason: pattern-konsistent zu 3.7-P2; echter-Prisma-Test-Pass für alle Eigenschutz-Read-Repos (3.7-OverdueQuery + 3.8-PushRecipient + ggf. zukünftige) zusammen in eigener Test-Infrastructure-Story (Test-Container-Pattern). Defense-in-depth via Set-Dedup + Schema-Drift-`userId`-Filter im Repo lindert Risiko bis dahin.

## Deferred from: code review of 415-3-9-sync-konflikt-erkennung-auf-psa-profil-backend (2026-05-04)

- **Bulk-Toggle annotiert nur ersten Konflikt** [`change-psa-profil.handler.ts` Bulk-Loop] — Wenn 3 Profile gleichzeitig getoggelt werden und Toggle 1 + Toggle 3 collidieren, kennt der `useChangePsaProfil`-Hook nur die zuweisungId von Toggle 1; Toggle 3 wird nie nach `sync_conflicts` gemeldet. Story-3.2 break-on-first-Pattern; Phase-2 Bulk-Conflict-Story erforderlich.
- **Tab-Reload-Race zwischen 409 und nicht-awaitetem `reportSyncConflict.mutate`** [`queries.ts:2865`] — Tab-Close zwischen 409-Response und HTTP-Folgecall verliert den Audit-Eintrag. Architektur §B6 spezifiziert Best-Effort-Charakter; UI-Echo via WS aus Domain-Event, das nur emittiert wird, wenn Folgecall durchläuft — Race ist designed.
- **Doppelklick-Mutation ohne `mutationKey`-Dedup** [`useReportSyncConflict`] — Backend ist idempotent auf `(einsatzId, entityId, localExpectedVersion, reportedByUserId)`, aber `localPayload` der zweiten Mutation geht verloren (erste gewinnt). Vertrag `i-sync-conflict.repository.ts:818` definiert das so; Story 3.10 zeigt verzerrten Loser-State, falls User zwischen Klicks toggled.
- **Auto-Dismiss `remaining===0`-Flicker** [`KonfliktErkanntMikroBanner.tsx:3239`] — Notice älter als 30 s beim Mount (Tab-Background-Throttle, Re-Mount) führt zu 1-Frame-Flash. Selten; `Math.max(0, …)` bereits gesetzt.
- **`parseConflictAnnotations` matched leeres `:einheit=` zu `''`** [`psa-profil.controller.ts:~660`] — Theoretisch unmöglich (`einheitId: string` aber kein NonEmptyString-Branding); 400-Path durch DTO-Validation gesichert; silentError schluckt.
- **WS-Hook `setStatus('error')` nach `MAX_RETRIES` ohne UI-Recover-Pfad** [`use-eigenschutz-konflikt-erkannt-live.ts:3120`] — User mit 10× transienten Connect-Failures verliert Live-Banner permanent. Pattern Story 3.6 1:1; Phase-2-UX-Story für globalen Reconnect-Banner.
- **`annotateConflictWithEinheitAndZuweisung` `zuweisungId`-Parameter ungenutzt** [`change-psa-profil.handler.ts:51`] — Optionaler Parameter wird vom einzigen Caller nie befüllt; Aggregate/Repo schreiben den Suffix bereits, Helper-Defense `!annotated.includes(':zuweisungId=')` greift. Reine Wartbarkeitsfalle / Naming-Mismatch.
- **Repository-Spec mit jest.fn()-Mock-Prisma statt echter Test-DB** [`prisma-sync-conflict.repository.spec.ts`] — AC3 fordert „echte Prisma-Test-Helpers"; Implementation folgt 3.7/3.8-D4-Pattern (mock-only). Defer auf Test-Infrastructure-Story (Test-Container-Pattern).
- **`localPayload` zyklische Refs / BigInt → `JSON.stringify` crasht** [`prisma-sync-conflict.repository.ts:1510`] — Plain-Object-Vertrag in Repo-Doc; Frontend baut nur plain Toggles. Edge nicht reproduzierbar ohne Frontend-Bug.
