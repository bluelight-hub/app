# Epic 3 Retrospektive: Fahrzeug-Einsatz-Verwaltung

**Datum:** 2025-12-18
**Facilitator:** Bob (Scrum Master)
**Epic Status:** DONE (4/4 Stories, 100%)

---

## Epic Summary

**Goal:** Sandra (FüKw) kann Fahrzeuge aus Stammdaten erfassen, temporäre Fahrzeuge anlegen, FMS-Status updaten - alles wird automatisch im ETB dokumentiert.

### Delivery Metrics

| Metrik | Wert |
|--------|------|
| **Completed Stories** | 4/4 (100%) |
| **Unit Tests** | 150+ (Aggregate + Handler Tests) |
| **Technical Debt Items** | 3 (DRY Violation, ETB Handler Tests, Fahrzeugtyp aktiv-Check) |
| **Code Review Rounds** | Story 3-2: 5 Rounds, Story 3-3: 3 Rounds |
| **Patterns etabliert** | Snapshot-Pattern, Fire-and-Forget ETB, FMS-Status Farbcodierung |

### Stories Completed

| Story | Beschreibung | Status | Highlights |
|-------|--------------|--------|------------|
| 3-0 | Prisma Schema EinsatzFahrzeuge | done | Snapshot-Pattern, Unique Constraint |
| 3-1 | Fahrzeug aus Stammdaten erfassen | done | TransactionalCommandHandler, ETB Auto-Creation, 54 Tests |
| 3-2 | Temporaeres Fahrzeug anlegen | done | stammId undefined semantics, 5 Review-Rounds |
| 3-3 | FMS-Status updaten | done | FmsStatusGeaendert Event, Frontend Components, 106 Tests |

---

## Was lief gut

### Pattern-Reuse aus Epic 1+2
Das TransactionalCommandHandler Pattern hat sich bewaehrt. Stories 3-1 bis 3-3 waren deutlich schneller implementiert als vergleichbare Stories in frueheren Epics. Das Outbox-Pattern funktioniert zuverlaessig - kein einziger Fall von orphaned Events.

### Story-Dokumentation
Die Dev Notes mit Anti-Patterns und Code-Referenzen waren extrem hilfreich. Besonders die Tabellen mit "Richtig vs. Falsch" und die Implementation Checklists haben viel Recherche erspart.

### Test-Coverage
Ueber 150 Unit Tests fuer Epic 3. Das AAA-Pattern mit Given-When-Then Kommentaren macht die Tests lesbar. Story 3-1 allein hat 54 Tests, Story 3-3 hat 106 Tests.

### ETB Auto-Creation
Alle ETB-Eintraege werden automatisch erstellt - kritisches Feature fuer Audit-Compliance. Fire-and-Forget Pattern verhindert, dass ETB-Fehler den Hauptworkflow blockieren.

### FMS-Status UI
FMS-Status-Dropdown mit Farbcodierung nach BOS-Standard sieht professionell aus. Optimistic Updates sorgen fuer gefuehlte Instant-Response.

---

## Was war schwierig

### Viele Review-Rounds
Die Code-Review-Rounds waren intensiv:
- Story 3-2: **5 Rounds** (CRITICAL: DTO Position Validation, Frontend Duplikat-Validation, ETB Adapter try/catch)
- Story 3-3: **3 Rounds** (CRITICAL: Idempotenz-Bug, Logger AC3, Test Mocks)

Viele Issues waren aehnlich: fehlende Validierungen, Fire-and-Forget nicht korrekt implementiert, Frontend-Komponenten nicht in UI integriert.

### undefined vs null Semantik
Die Unterscheidung `stammId: undefined` vs `stammId: null` war verwirrend:
- Domain nutzt `undefined`
- Prisma/DB nutzt `null`
- Mapper muss konvertieren: `(x as T | null) ?? undefined`

### ETB Handler Tests fehlen
Fire-and-Forget Pattern ist schwer zu testen. Ohne Tests wissen wir nicht sicher, ob ETB-Eintraege wirklich erstellt werden.

### DRY Violation
~75 Zeilen Duplikation zwischen `createFromStammdaten()` und `createTemporary()` im EinsatzFahrzeug Aggregate. Das ist Technical Debt.

---

## Key Learnings

### 1. Snapshot-Pattern fuer Einsatzdaten (NEU - KRITISCH!)

```typescript
// RICHTIG: Daten KOPIEREN bei Erfassung
const einsatzFahrzeug = EinsatzFahrzeug.createFromStammdaten({
  funkrufname: stammFahrzeug.funkrufname,  // KOPIE
  kennzeichen: stammFahrzeug.kennzeichen,  // KOPIE
  stammId: stammFahrzeug.id,               // Rueckverfolgbarkeit
});

// FALSCH: Live-Referenz auf Stammdaten
// Wenn StammFahrzeug geaendert wird, aendert sich EinsatzFahrzeug NICHT!
```

**Warum:** Einsatzdaten sind ein Snapshot zum Erfassungszeitpunkt. Spaetere Aenderungen an Stammdaten duerfen historische Einsaetze nicht veraendern.

### 2. undefined vs null Semantik

| Kontext | Wert | Bedeutung |
|---------|------|-----------|
| Domain Aggregate | `undefined` | Nicht gesetzt |
| Prisma/DB | `null` | Nicht gesetzt |
| Mapper | `(x as T \| null) ?? undefined` | Konvertierung |

**Beispiel im Mapper:**
```typescript
// toDomain
stammId: (prismaEntity.stammId as string | null) ?? undefined,

// toPrisma
stammId: aggregate.stammId ?? null,
```

### 3. Fire-and-Forget Pattern fuer ETB

```typescript
// ETB Handler: Fehler loggen, NICHT propagieren
async handle(event: FahrzeugErfasst): Promise<void> {
  try {
    await this.etbService.createEintrag({
      text: `Fahrzeug ${event.funkrufname} erfasst`,
      kategorie: 'FAHRZEUG',
    });
  } catch (error) {
    this.logger.error('ETB creation failed', error);
    // KEINE Exception werfen - Fire-and-Forget!
  }
}
```

**Warum:** ETB-Fehler duerfen den Hauptworkflow (Fahrzeug erfassen) nicht blockieren.

### 4. FMS-Status Farbcodierung (BOS-Standard)

| Status | Label | Farbe | Tailwind Light | Tailwind Dark |
|--------|-------|-------|----------------|---------------|
| 0 | Nicht einsatzbereit | Grau | `bg-gray-100 text-gray-800` | `dark:bg-gray-800 dark:text-gray-200` |
| 1 | Auf Wache | Grau | `bg-gray-100 text-gray-800` | `dark:bg-gray-800 dark:text-gray-200` |
| 2 | Einsatzbereit | Gruen | `bg-green-100 text-green-800` | `dark:bg-green-900/30 dark:text-green-300` |
| 3 | Ausgerueckt | Blau | `bg-blue-100 text-blue-800` | `dark:bg-blue-900/30 dark:text-blue-300` |
| 4 | Am Einsatzort | Gelb | `bg-yellow-100 text-yellow-800` | `dark:bg-yellow-900/30 dark:text-yellow-300` |
| 5 | Sprechwunsch | Orange | `bg-orange-100 text-orange-800` | `dark:bg-orange-900/30 dark:text-orange-300` |
| 6-9 | Ausser Dienst/Regional | Violett | `bg-purple-100 text-purple-800` | `dark:bg-purple-900/30 dark:text-purple-300` |

### 5. Domain Event Idempotenz

```typescript
// RICHTIG: Event nur bei tatsaechlicher Aenderung emittieren
updateFmsStatus(props: UpdateFmsStatusProps): Result<void> {
  const statusChanged = this._fmsStatus !== props.fmsStatus;

  // Validierung und Mutation...

  if (statusChanged) {
    this.addDomainEvent(new FmsStatusGeaendertEvent(...));
    this.updateTimestamp(props.updatedBy);
  }

  return Result.ok();
}
```

**Warum:** Idempotente Handler verhindern doppelte ETB-Eintraege bei Retries.

---

## Epic 2 Action Items Follow-Through

| # | Action Item | Status | Ergebnis |
|---|-------------|--------|----------|
| 1 | DI Token Export Pattern dokumentieren | DONE | In project-context.md |
| 2 | Rate Limit Guidelines definieren | DONE | 120/min fuer Read-Endpoints |

---

## Technical Debt

| # | Debt Item | Severity | Story | Empfehlung |
|---|-----------|----------|-------|------------|
| 1 | DRY Violation EinsatzFahrzeug Aggregate (~75 Zeilen) | HIGH | 3-2 | Private `validateCreateProps()` Methode extrahieren |
| 2 | ETB Handler Tests fehlen | HIGH | 3-2, 3-3 | Unit Tests fuer Fire-and-Forget Pattern, Temporaer-Check, Error Cases |
| 3 | Fahrzeugtyp aktiv-Check fehlt | MEDIUM | 3-2 | `if (!fahrzeugtyp.istAktiv)` Check + FAHRZEUGTYP_INACTIVE Error Code |
| 4 | UUID-Validierung fuer einsatzId fehlt | LOW | 3-2 | UUID-Regex oder `uuid` package Validierung im Domain Aggregate |
| 5 | Hardcoded CUID2 Fehlermeldungen | LOW | 3-2 | EINSATZ_FAHRZEUG_VALIDATION_ERRORS erweitern |

---

## Action Items

### Process Improvements

| # | Action Item | Owner | Priority |
|---|-------------|-------|----------|
| 1 | Review-Checklist erstellen (Fire-and-Forget, undefined/null, UI-Integration) | Dev | HIGH |
| 2 | ETB Handler Tests nachholen - vor Epic 4 Start | Dev | HIGH |
| 3 | DRY Refactoring EinsatzFahrzeug Aggregate | Dev | MEDIUM |
| 4 | Fahrzeugtyp aktiv-Check implementieren | Dev | MEDIUM |

### Team Agreements fuer Epic 4

1. **Snapshot-Pattern:** Einsatz-Personen KOPIEREN Daten aus StammPerson (funkrufname wird NICHT live referenziert)
2. **Fire-and-Forget:** Alle ETB Handler mit try/catch + Logging implementieren
3. **UI-Integration pruefen:** Neue Komponenten MUESSEN in der UI gerendert werden (nicht nur erstellt)
4. **Tests first fuer ETB:** ETB Handler Tests schreiben BEVOR Feature deployed wird
5. **Idempotenz:** Domain Events nur bei tatsaechlicher Aenderung emittieren

---

## Epic 4 Vorbereitung

### Dependencies (alle erfuellt)

| Dependency | Status |
|------------|--------|
| `EinsatzFahrzeug` Entity (Epic 3) | Verfuegbar |
| `StammPerson` Entity (Epic 2) | Verfuegbar |
| `Qualifikation` Entity (Epic 1) | Verfuegbar |
| TransactionalCommandHandler Pattern | Etabliert |
| Fire-and-Forget ETB Pattern | Etabliert |
| Snapshot-Pattern | Etabliert |

### Epic 4 Stories

| Story | Beschreibung | Dependencies |
|-------|--------------|--------------|
| 4-0 | Prisma Schema Einsatz-Personen | Story 3-0 (Pattern), Story 2-0 (StammPerson) |
| 4-1 | Person manuell registrieren | Story 4-0, Story 2-2 (StammPerson) |
| 4-2 | Person via QR-Code registrieren | Story 4-1, DRK-Helfer-App Format |
| 4-3 | Person zu Fahrzeug zuweisen | Story 4-1, Story 3-1 (EinsatzFahrzeug) |

### Neue Komplexitaet in Epic 4

- **QR-Code Scanning:** DRK-Helfer-App Format dekodieren (FR41)
- **Person-Fahrzeug Zuweisung:** M:N Relation zwischen EinsatzPerson und EinsatzFahrzeug
- **Automatischer DB-Lookup:** QR-Code -> StammPerson matchen (FR10)
- **Performance:** <3 Sekunden fuer kompletten QR-Flow (NFR3)
- **Temporaere Personen:** Ohne Stammdaten-Referenz (spontane Helfer)

### Blocker Status

**Keine Blocker fuer Epic 4!** Alle Dependencies sind erfuellt.

---

## Retrospective Closure

### Key Takeaways

1. **Snapshot-Pattern ist Standard** - Einsatzdaten KOPIEREN, nicht referenzieren
2. **Fire-and-Forget funktioniert** - ETB-Fehler blockieren nicht den Hauptworkflow
3. **Idempotenz ist wichtig** - Events nur bei tatsaechlicher Aenderung emittieren
4. **Review-Checklist wuerde helfen** - Viele Issues waren wiederkehrend
5. **Test-Coverage ist gut** - 150+ Tests zeigen Commitment

### Team Performance

Epic 3 lieferte 4 Stories mit 150+ Unit Tests. Die intensiven Review-Rounds (5 fuer Story 3-2, 3 fuer Story 3-3) zeigen gruendliche Qualitaetskontrolle. Das Team hat alle kritischen Issues behoben und ist gut aufgestellt fuer Epic 4.

### FRs Delivered

| FR | Beschreibung | Story |
|----|--------------|-------|
| FR1 | Fahrzeuge aus Stammdaten erfassen | 3-1 |
| FR2 | Temporaere Fahrzeuge anlegen | 3-2 |
| FR3 | FMS-Status updaten (0-9) | 3-3 |
| FR4 | Fahrzeugtypen nach DIN EN 1789 anzeigen | 3-1, 3-2 |
| FR5 | Fahrzeug-Attribute tracken | 3-0 |
| FR27 | Auto-ETB bei Fahrzeug-Statusaenderung | 3-3 |
| FR29 | Auto-ETB bei Kraefte-Erfassung | 3-1, 3-2 |
| FR30 | ETB-Eintraege immutable | 3-1, 3-2, 3-3 |
| FR31 | Outbox Pattern fuer ETB-Persistierung | 3-1, 3-2, 3-3 |

### Next Steps

1. sprint-status.yaml aktualisiert (epic-3-retrospective: completed)
2. Retrospektive-Dokument gespeichert
3. Epic 4 kann starten - keine Blocker

---

**Retrospective Status:** Completed
**Document:** `docs/sprint-artifacts/epic-3-retro-2025-12-18.md`
**Generated:** 2025-12-18
