# Versioning Strategy

**VERSION_NEUTRAL:**
- `/api/auth/*`
- `/api/health/*`
- `/api/` (Root)

**alpha:**
- `/api/alpha/einsatz/*`
- `/api/alpha/users/*`
- `/api/alpha/admin/users/*`

**Note:** ETB-Endpunkte verwenden derzeit keine Versionierung (`/api/etb/*`).

---
