import { useState, useEffect } from 'react';
import { Navigate } from '@tanstack/react-router';
import { PiPlus, PiWarning } from 'react-icons/pi';
import { useAdminAuth } from '@/features/auth/api';
import { useAdminRollenDefinitionenManagement, type RollenDefinitionDto, type CreateRollenDefinitionDto, type UpdateRollenDefinitionDto } from '@/features/admin/api';
import { Button } from '@/shared/ui/atoms/button.atom';
import { Card } from '@/shared/ui/atoms/card.atom';
import { Container } from '@/shared/ui/atoms/container.atom';
import { Heading } from '@/shared/ui/atoms/heading.atom';
import { Text } from '@/shared/ui/atoms/text.atom';
import { Skeleton } from '@/shared/ui/atoms/skeleton';
import { ErrorBoundary } from '@/shared/ui/organisms/ErrorBoundary';
import { RollenDefinitionenTable } from '../organisms/RollenDefinitionenTable';
import { CreateRollenDefinitionDialog } from '../organisms/CreateRollenDefinitionDialog';
import { EditRollenDefinitionDialog } from '../organisms/EditRollenDefinitionDialog';
import { DeactivateRollenDefinitionDialog } from '../organisms/DeactivateRollenDefinitionDialog'; /** * Admin Rollen-Definitionen Page. * * Zeigt Tabelle aller Rollendefinitionen mit CRUD-Operationen. * Nur fuer authentifizierte Admins zugaenglich. * * Keyboard Shortcuts: * - Ctrl+N / Cmd+N: Neue Rollendefinition erstellen */
export function AdminRollenDefinitionen() {
  const { isAdmin, isLoading: isAuthLoading } = useAdminAuth();
  const {
    rollenDefinitionen,
    isLoading: isRollenDefinitionenLoading,
    error,
    refetch,
    createRollenDefinition,
    updateRollenDefinition,
    deactivateRollenDefinition,
    isCreating,
    isUpdating,
    isDeactivating,
    updatingId,
    deactivatingId,
  } = useAdminRollenDefinitionenManagement(); // Dialog States const [isCreateDialogOpen, setIsCreateDialogOpen] = useState(false); const [isEditDialogOpen, setIsEditDialogOpen] = useState(false); const [editTarget, setEditTarget] = useState<RollenDefinitionDto | null>(null); const [deactivateTarget, setDeactivateTarget] = useState<RollenDefinitionDto | null>(null); // Keyboard Shortcuts: Ctrl+N / Cmd+N oeffnet Create Dialog useEffect(() => { const handleKeyDown = (event: KeyboardEvent) => { // Ctrl+N (Windows/Linux) oder Cmd+N (Mac) if ((event.ctrlKey || event.metaKey) && event.key === 'n') { event.preventDefault(); // Verhindert Browser "Neues Fenster" setIsCreateDialogOpen(true); } }; window.addEventListener('keydown', handleKeyDown); // Cleanup: Event Listener entfernen beim Unmount return () => { window.removeEventListener('keydown', handleKeyDown); }; }, []); // Leeres Dependency Array - nur beim Mount/Unmount // Auth Guard if (!isAuthLoading && !isAdmin) { return <Navigate to="/admin-login" />; } // Handlers const handleCreateRollenDefinition = (data: CreateRollenDefinitionDto) => { createRollenDefinition(data, { onSuccess: () => { setIsCreateDialogOpen(false); }, }); }; const handleEditRollenDefinition = (rollenDefinition: RollenDefinitionDto) => { setEditTarget(rollenDefinition); setIsEditDialogOpen(true); }; const handleUpdateRollenDefinition = (data: UpdateRollenDefinitionDto) => { if (!editTarget) return; updateRollenDefinition( { id: editTarget.id, data }, { onSuccess: () => { setIsEditDialogOpen(false); setEditTarget(null); }, }, ); }; const handleDeactivateRollenDefinition = (rollenDefinition: RollenDefinitionDto) => { setDeactivateTarget(rollenDefinition); }; const confirmDeactivate = () => { if (!deactivateTarget) return; deactivateRollenDefinition(deactivateTarget.id, { onSuccess: () => { setDeactivateTarget(null); }, }); }; // Loading State if (isAuthLoading || isRollenDefinitionenLoading) { return ( <Container maxWidth="6xl" className="py-8"> <div className="flex flex-col gap-6"> {/* Header Skeleton */} <div className="flex items-center justify-between"> <div className="flex flex-col gap-2"> <Skeleton className="h-8 w-48" /> <Skeleton className="h-4 w-96" /> </div> <Skeleton className="h-10 w-48" /> </div> {/* Table Skeleton */} <Card padding="none"> <div className="p-6"> <div className="space-y-4"> {[...Array(5)].map((_, index) => ( // eslint-disable-next-line react/no-array-index-key -- Static skeleton elements - order never changes <div key={`skeleton-${index}`} className="flex items-center gap-4"> <Skeleton className="h-6 w-24" /> <Skeleton className="h-6 w-32" /> <Skeleton className="h-6 w-48" /> <Skeleton className="h-6 w-20" /> <Skeleton className="h-6 w-16" /> </div> ))} </div> </div> </Card> </div> </Container> ); } // Error State if (error) { return ( <Container maxWidth="6xl" className="py-8"> <Card padding="lg" className="text-center"> <div className="flex flex-col items-center gap-4"> <PiWarning className="h-12 w-12 text-status-danger-text" /> <Heading size="md">Fehler beim Laden</Heading> <Text className="text-text-secondary">Die Rollendefinitionen konnten nicht geladen werden.</Text> <Text className="text-text-muted text-sm">{error.message}</Text> <Button onClick={() => void refetch()} intent="primary" loading={isRollenDefinitionenLoading} disabled={isRollenDefinitionenLoading}> Erneut versuchen </Button> </div> </Card> </Container> ); } return ( <ErrorBoundary> <Container maxWidth="6xl" className="py-8"> <div className="flex flex-col gap-6"> {/* Header */} <div className="flex items-center justify-between"> <div> <Heading size="lg" as="h1"> Rollen-Definitionen </Heading> <Text className="text-text-secondary">Verwalten Sie die verfügbaren Rollendefinitionen für die taktische Übersicht.</Text> </div> <Button onClick={() => setIsCreateDialogOpen(true)} intent="primary" appearance="outline"> <PiPlus className="mr-2" /> Neue Rolle </Button> </div> {/* Table */} <Card padding="none"> <RollenDefinitionenTable rollenDefinitionen={rollenDefinitionen || []} isLoading={isRollenDefinitionenLoading} onEdit={handleEditRollenDefinition} onDeactivate={handleDeactivateRollenDefinition} updatingId={updatingId} deactivatingId={deactivatingId} /> </Card> </div> {/* Dialogs */} <CreateRollenDefinitionDialog isOpen={isCreateDialogOpen} onClose={() => setIsCreateDialogOpen(false)} onSubmit={handleCreateRollenDefinition} isSubmitting={isCreating} /> <EditRollenDefinitionDialog isOpen={isEditDialogOpen} onClose={() => { setIsEditDialogOpen(false); setEditTarget(null); }} onSubmit={handleUpdateRollenDefinition} isSubmitting={isUpdating} rollenDefinition={editTarget} /> <DeactivateRollenDefinitionDialog isOpen={!!deactivateTarget} onClose={() => setDeactivateTarget(null)} onConfirm={confirmDeactivate} rollenDefinition={deactivateTarget} isSubmitting={isDeactivating} /> </Container> </ErrorBoundary> );
}
