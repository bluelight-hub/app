# API Configuration
# Standard mit den lokalen Zertifikaten aus `pnpm run gen:certs`
VITE_API_URL=https://localhost:3091

# Frontend Dev Server Port (default: 3090)
# In Worktrees automatisch via scripts/worktree-setup.sh gesetzt
# VITE_PORT=3090

# Für HTTP-only lokal stattdessen:
# VITE_API_URL=http://localhost:3091

# Security Mode
# Wenn 'true', sind http:// Verbindungen erlaubt (nur für Entwicklung!)
# In Produktion sollte dies NIEMALS gesetzt sein.
# VITE_INSECURE_MODE=true

# ==== Web-Push (Story 1.2, ADR-011) ====
# Public-Key des Backend-VAPID-Keypairs (Bootstrap für den Service-Worker).
# Wird erst in Story 1.2 (Service-Worker + Tauri-Bridge) aktiv verwendet,
# kann aber schon jetzt gesetzt werden, damit lokale Dev-Setups vollständig sind.
# Der Private-Key bleibt AUSSCHLIESSLICH im Backend.
VITE_VAPID_PUBLIC_KEY=
