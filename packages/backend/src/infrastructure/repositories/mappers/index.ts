/**
 * Barrel Export für Prisma Mappers (Infrastructure Layer).
 *
 * Diese Datei exportiert alle Mapper-Klassen für Domain ↔ Prisma Transformation.
 * Ermöglicht saubere Imports: `import { PrismaPoiMapper } from '@infrastructure/repositories/mappers'`
 */

export { PrismaPoiMapper } from './prisma-poi.mapper';
export { PrismaLagekarteMapper } from './prisma-lagekarte.mapper';
