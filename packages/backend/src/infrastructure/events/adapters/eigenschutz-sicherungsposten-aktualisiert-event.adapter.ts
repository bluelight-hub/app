/**
 * Slug-Proxy für den Feature-Slice-Adapter (Story 4.1).
 *
 * Pendant zu `eigenschutz-sicherungsposten-eingerichtet-event.adapter.ts` —
 * existiert ausschließlich, damit die Konsistenz-Spec die Registrierung an
 * Stelle 4 (Slug `eigenschutz-sicherungsposten-aktualisiert`) findet.
 */
export { EigenschutzSicherungspostenAktualisiertEventAdapter } from '@/infrastructure/eigenschutz/event-adapters/sicherungsposten-aktualisiert.adapter';
