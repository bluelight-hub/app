import { EntityId } from '@domain/common/entity-id';

export class ErinnerungKonfigurationId extends EntityId<'ErinnerungKonfiguration'> {
  // Factory methods are inherited but strict typing is enforced by generic
}
